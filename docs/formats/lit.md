# LIT и PIT — база точек интереса

**Актуальная проверка (20 сентября 2026).** Машина грамматики дочитывает до
точной границы **все 245 518 блоков LIT** и **все 717 блоков PIT**. Проверка
меряет **границы чтения**, а не правильность полей: сошлось число прочитанных
байт. Обход дерева POI и генератор кипрского LIT не сделаны.

Что привело сюда, по порядку: выход из пустого цикла `0xa2`, полная чистка
кадра при вызове и при переходе `0x11`, точные швы между томами и чтение длин
без знака. Самым крупным оказался шов: наивная склейка «размер файла минус
обёртка» ошибалась на выравнивающую добивку, и три четверти каталога читались
со сдвигом — это выглядело как «каталог зонирован». Подробности и
воспроизведение — в последних разделах. Ниже сохранён журнал исследования:
ранние гипотезы и оценки могут быть опровергнуты поздними.

### LIT и PIT — один контейнер, магия `Lit\x02`

Блоб внутри LIT-контейнера и файл PIT целиком начинаются одинаково, и заголовки
**совпадают побайтно** — различаются только две метки времени:

```
+0   4б    "Lit\x02"          магия
+4   u32   0x00010140         версия или флаги
+8   u32   метка времени      0x6211D861 — 20 февраля 2022
+12  4б    "HBIS"             Harman Becker
+16  u32   та же метка времени
+20  2б    "NT"               NavTeq
+24  u16   0x07E6 = 2022
+32  ...   таблица по 8 байт: (320,1) (32,352) (2,8) (368,1)
```

Таблица с +32 у LIT и PIT **одна и та же**, что подтверждает: формат общий.

**Практическое следствие.** `PIT` весит 8.9 МБ, а `LIT` — 2 ГБ на том. Разобрав
маленький PIT, мы автоматически получаем разбор LIT. Это делает PIT самым
дешёвым входом в задачу названий улиц.

Сжатия в блобе нет: в первых 64 КБ нашлось всего две сигнатуры zlib, то есть
данные лежат россыпью, а не общим потоком. При этом распаковщик zlib в прошивке
есть (строки `invalid literal/lengths set`, `invalid literal/length code`) —
вероятно, для отдельных строковых таблиц.

**Читатель в прошивке не найден.** Поиск по строкам не дал модуля, который
разбирает `Lit\x02`. Регистрация `.LIT`/`.LI2`…`.LI9` в диспетчере NDL ведёт
лишь к `NavCore\ndl\private\CCDMSingleFileLoader.cpp` — это раздача описателей
(до 40 штук по 20 байт), а не разбор формата. Где лежит собственно читатель,
пока неизвестно.

### Контейнер `Lit\x02` разобран по данным (на образце PIT)

`PIT` весит 8.9 МБ против 2 ГБ у `LIT`, а формат у них общий — поэтому разбор
вёлся на нём.

**Заголовок.**

```
+0   4б    "Lit\x02"
+4   u32   0x00010140             версия или флаги
+8   u32   метка времени          0x62130921 — 20 февраля 2022
+12  4б    "HBIS"                 Harman Becker
+16  u32   та же метка
+20  2б    "NT"                   NavTeq
+24  u16   0x07E6 = 2022
+32  ...   таблица пар u32 BE (см. ниже)
+80  ...   "Europe V7.0"          название набора
```

**Таблица пар с +32** (у LIT и PIT одинакова в первых четырёх строках):

```
(320, 1)         320    -> строка с префиксом длины:  "DVD"
(32, 352)        352    -> метки TTD и HBIS со счётчиками
(2, 8)
(368, 1)         368    -> TTD, затем (384, 7880)
(16, 8264)       8264   -> ИНДЕКС
(717, 262662)    717    -> число записей индекса
```

**Индекс подтверждён арифметикой и данными.** 717 записей по 6 байт начиная с
8264: `u32 BE смещение` + `u16`. Конец индекса `8264 + 717*6 = 12566` — ровно
совпадает с первым смещением в нём. Проверка:

```
монотонно растут    717 из 717
внутри файла        717 из 717
последнее смещение  8 868 837  при размере файла 8 869 262
длины кусков        минимум 337, медиана 13 438, максимум 32 468
```

Сходится без единого исключения.

**Что внутри кусков.** В первом же куске видна россыпь двухбуквенных групп,
разделённых байтом `0x12`:

```
12 41 4e   12 49 54   12 4c 01   12 03 44   12 45 52   12 41 4c
   "AN"       "IT"       "L"        "D"        "ER"       "AL"
```

Это фрагменты имён — похоже на префиксное дерево или таблицу слогов для поиска
названий. Что согласуется с назначением: `LIT` это база подписей, а `PIT`,
судя по всему, указатель к ней.

Сжатия нет: на первые 64 КБ приходится всего две сигнатуры zlib.

### Внутри куска — словарь для сжатия имён

Кусок начинается с маленького заголовка в 13 байт
(`08 0a 05 18 00 00 00 00 00 00 00 00 79`), за которым идут записи с тегами:

```
0x12  + 2 байта
0x13  + 3 байта
0x22  — конец словаря (у первого куска на смещении 12675)
```

В первом куске таких записей **31**. Их содержимое — самые частые
буквосочетания европейских языков, и каждое встречается **ровно один раз**:

```
AN  IT  ER  AL  IA  RE  IN  OR  EL  FR  TR  US  SP  NA  EN  GI  " B"  MO
```

Вперемешку с ними идут записи, где буква стоит рядом с маленьким числом
(`L·01`, `·03 D`, `G·06`, `B·0e`, `·15 U`). Похоже на **рекурсивный словарь**:
значения меньше 0x20 — ссылки на прежние записи, остальное — буквы. Так
устроено сжатие вида LZW.

**Проверка разворачиванием — наполовину.** По правилу «меньше 0x20 значит
ссылка» получается:

```
0  "AN"     2  "LIT"    8  "ALER"   20  "PELT"    28  "LITIA"
1  "IT"     4  "ER"     9  "IN"     22  "GIA"     30  "LITIAEN"
```

Узнаваемые фрагменты есть — `LIT`, `GIA`, `ALER`, `TR`, `SP`, `EN`. Но запись 3
ссылается сама на себя (бесконечная рекурсия), а `TRITU`, `LITIAEN`, `SW<`,
`1'`, `>;` выглядят мусором. Значит правило близко, но неточно: возможно, порог
ссылки другой, либо ссылки относительные, либо словарь предзаполнен и наши
номера смещены.

**Правило ссылки проверено на 25 кусках — не найдено.** Испытаны пять
вариантов, оценка — доля записей, разворачивающихся в чистые буквы:

```
v < 0x20 -> запись v-1     48.6%
v < 0x20 -> запись i-v     47.6%
v < 0x41 -> запись i-v     45.9%
v < 0x20 -> запись v       44.9%
v < 0x41 -> запись v       43.1%
```

Контроль (не разворачивать вовсе, все байты считать буквами) даёт **32.8%**,
а печатных байтов в записях 79.3%. То есть разворачивание улучшает картину с
32.8% до 48.6% — прибавка настоящая, но верное правило дало бы под сто
процентов. Вывод: сама идея словаря со ссылками подтверждается, а конкретное
правило неверно, либо неверна раскладка записи (теги `0x12`/`0x13` могут нести
больше смысла, чем «два байта» и «три байта»).

### LIT — это база POI, и она разобрана по декомпиляции

Название сбивало с толку: `LIT` — не «литералы», а **база точек интереса**.
В прошивке это `Q:\platform\common\isdb\lit\` из четырёх модулей, и каждый
печатает свою версию:

```
lit_main      3.3.6c    15.05.08
lit_loader    1.1.3     22.04.2008
lit_access    1.02.02   18.07.2007
lit_poitiles  1.03.02   11.12.2007
```

Читает её `CISGdbLitMainDirectory::openLit` — `FUN_08cc4f58`. Именно он дал
раскладку, которую я до того безуспешно угадывал.

#### Заголовок `Lit\x02`

Лежит не в начале файла, а внутри обёртки FLDB: у `LIT` на смещении 2048, у
`PIT` — на нуле. Магия читается как big-endian u32 `0x4C697402`; альтернативная
ветка читателя ждёт `0xABADCAFE`, в наших файлах её нет.

```
+0   u32  магия 'Lit\x02'
+4   u16  версия минус 11      (у нас 1 -> DatabaseVersion 12, допустимо 3..13)
+8   u32  timestamp
+56  u32  смещение подзаголовка 'TTD\0'
+68  u32  начало каталога
+72  u32  число блоков
+76  u8   смещение поля внутри записи
+77  u8   ширина поля «смещение блока», байт
+78  u8   ширина поля «размер блока», байт
+79  u8   размер записи        (при версии 13 заголовок на 2 байта длиннее)
```

#### Каталог

Запись каталога — это просто **смещение блока и его размер**, блоки лежат
вплотную. Ширина полей объявлена в заголовке, и она разная:

| | записей | запись | смещение | размер |
|---|---|---|---|---|
| `LIT` | 245 518 | 7 байт | **5 байт** (40 бит) | 2 байта |
| `PIT` | 717 | 6 байт | 4 байта | 2 байта |

Пятибайтовое смещение у `LIT` — и есть причина, по которой прошлые попытки
провалились: я перебирал записи по 6 и 8 байт, а она семибайтовая, потому что
адресное пространство в 6.59 ГБ в 32 бита не влезает.

Проверка сходится сама с собой с двух концов, без единого допущения:

```
LIT: 245518 блоков, разрывов в цепочке 0 из 245517
     первый блок @1726890 == конец каталога 8264 + 245518·7
     последний кончается на 7073296007 — ровно сумма четырёх томов
PIT: 717 блоков, разрывов 0 из 716
     первый @12566 == конец каталога 8264 + 717·6
     последний кончается на 8869262 — ровно размер файла
```

Размер блока у `LIT` от 2 до 62 КБ, у `PIT` до 65 КБ, и всегда меньше 65536 —
что подтверждается утверждением в самой прошивке:
`vte.getCompressedSize() < 0x10000`.

#### Четыре тома — один адрес

Магия `Lit\x02` есть только у `L1`; `LIT2`, `LIT3`, `LIT4` начинаются прямо с
данных. Это один логический том из четырёх частей — как `.gdb` + `.gd2` у GDB:

```
L1   0 … 2 147 399 680
L2   2 147 399 680 … 4 294 780 928
L3   4 294 780 928 … 6 442 180 608
L4   6 442 180 608 … 7 073 300 480
```

#### Читатель `src/lit.js`

`open(dirs)` находит магию, разбирает заголовок, склеивает тома в сквозное
адресное пространство и отдаёт `catalog()` и `block(e)`. Проверен на обоих
контейнерах — см. цифры выше.

#### Что такое POI в этой базе

Категории лежат прямо в прошивке: `TANKSTELLE`, `HOTEL/MOTEL`, `RESTAURANT`,
`PARKPLATZ/-HAUS`, `APOTHEKE`, `KRANKENHAUS`, `FLUGHAFEN`, `BAHNHOF`, `KINO`,
`MUSEUM`. Поля одной точки прошивка называет поимённо:

```
POIIndexName        POIIndexOriginalName   POIIndexBrand
POIIndexStreet      POIIndexHouseNumber    POIIndexPostalArea
POIIndexCityName    POIIndexCountry        POIIndexGeoPos
POIIndexPhoneNumber POIIndexFaxNumber      POIIndexFoodType
POIIndexType        POIIndexSubType        POIIndexParkingType
```

В сырых данных `LIT3` бренды видны глазом: на четырёх мегабайтах — `HOTEL` 13
раз, `LIDL` 9, `PARK` 6, `ESSO` 3, `MCDONALD` 1. Мало потому, что имена сжаты
словарём.

**Для кипрской карты POI нужны** — решение пользователя. Причём оно общее:
«да всё нужно». Ничего из набора не выбрасывается — ни `LIT` (точки интереса),
ни `CTY` (трёхмерные здания), ни `TER` (рельеф). Для каждого компонента надо
научиться **генерить** кипрское содержимое.

#### Запись после разбора — построчный текст

`getDetailLine` найден: **`0x08c9e3b8`** (Ghidra в этом участке функций не
создала, дизассемблировал capstone'ом). Он устроен просто: берёт у разборщика
указатель на текст и идёт по строкам, сравнивая **первый байт строки** с
запрошенным тегом; при совпадении копирует остаток строки до `\n` (0x0A) или до
нуля, не больше **254** байт.

Значит декодированная запись POI — это обычный текст вида
`<тег><значение>\n<тег><значение>\n…`, а `linetag` выбирает поле. Теги —
однобайтовые; в диспетчере разборщика видны сравнения с 91 `[`, 70 `F`, 41 `)`,
38 `&`, 37 `%`.

#### Словарь: как именно разворачиваются коды

Цепочка: `getDetailLine` -> `FUN_08ccaf60` (ленивый доступ: если взведён флаг по
смещению 2428, вызвать декодер и сбросить) -> **`FUN_08ccfcc0` — сам декодер**.

Декодер идёт по буферу и для каждого байта `b` смотрит две таблицы в объекте
разборщика:

```
+250  u16   число кодов
+252  256×u8   длина раскрытия для кода b   (0 — значит это обычный байт)
+508  256×u16  смещение раскрытия для кода b
+204  указатель на пул текста
```

Байт считается кодом, только если `b < число кодов` **и** `длина[b] != 0` —
поэтому заглавные буквы остаются буквами, а строчные могут быть кодами. Найдя
код, декодер сдвигает хвост буфера к концу и переписывает его обратно, подставляя
раскрытия; **подставленное перечитывается снова**, так что код может
раскрываться в другие коды. Это объясняет и ссылки внутри словарных записей, и
записи, обрывающиеся на середине символа UTF-8.

Наблюдение `BAHNnF` -> **BAHNHOF** этой схеме соответствует.

#### Словарь наполняется двумя типами записей

Наполнение нашлось в разборщике (`0x08ccfa6a`). Работают два типа записей,
и поле типа в объекте лежит по смещению 188:

* **тип 91** — задаёт **номер кода**, который будет определён следующим
  (кладётся в `this+1044`);
* **тип 93** — **определяет запись**: длина берётся из `this+180`, текст — с
  текущей позиции разбора, а в таблицу смещений кладётся `текущая позиция минус
  база пула`.

Если номер кода перескочил вперёд, промежуточным кодам длина обнуляется — то
есть пропущенные значения байтов остаются обычными литералами. Вот почему `A`-`Z`
остаются буквами, а `a`-`z` оказываются кодами: у одних длина 0, у других нет.
Счётчик `this+250` двигается только когда номер идёт подряд.

#### Внутри блока лежат ещё и транскрипции для синтеза речи

Обзор по 200 блокам показал, что доля печатаемых байт гуляет от 19 % до 88 %.
Самый «текстовый» блок (30685) раскрыл, что именно лежит внутри:

```
20118  m|.%C.Rt[a0|ka|'rre||'kor|Du|Ba|
20214  in|ti|'Dos|..._.CARRER D..0.'..E
20262  .II.0|ka|'rre||dal|'fon|so||'tre|Te|
```

`'rre||kris|'to|for||ko|'lom` — это **фонетическая транскрипция** каталанского
«Carrer de Cristòfor Colom», а `dal|'fon|so||'tre|Te|` — «d'Alfons XIII».
То есть `LIT` несёт не только названия, но и их произношение для синтеза речи
(в прошивке рядом лежит `libsvox.so`). Для Кипра это придётся генерить тоже.

Само название видно наполовину открытым текстом: `CARRER D` лежит как есть, а
продолжение — кодами. Это ровно та схема со словарём, что описана выше.

#### Разгадка: формат записей описан схемой в самом файле

Ghidra эту область не анализировала, из-за чего я трижды упирался в одно и то
же место. Решилось headless-анализом только диапазона `0x08ccf000..0x08cd1000`:
импорт бинаря в отдельный проект с `-noanalysis`, затем `DisassembleCommand` +
`AutoAnalysisManager.reAnalyzeAll(set)` и декомпиляция всех функций диапазона.
Появилась 21 функция, и среди них обе нужные.

Шаг разбора — `FUN_08ccf84c`. Он начинается с вызова `FUN_08cd0798`, который
читает очередную запись и возвращает номер случая; дальше `switch` раскладывает
значение по полям объекта. Там же видно то, что мы вывели по частям:

* случай 2 — простое поле; тип сравнивается с `0x5b` `[`, `0x25` `%`,
  `0x26` `&`, `0x29` `)`, `0x46` `F`;
* случаи 5 и 6 — при типе `0x5d` `]` добавляется запись словаря;
* случай 4 — пара координат со сдвигом.

**Главное — устройство `FUN_08cd0798`.** Он читает не фиксированный заголовок, а
идёт по **таблице-схеме**: массив 16-разрядных слов, у каждого младший байт —
код операции (`0x10`, `0x11`, `0x21`, `0x25`, `0x43`, …). То есть **файл несёт
собственную грамматику**, а разборщик — маленькая машина, которая её исполняет.
Именно поэтому все попытки угадать «тег плюс длина» проваливались: постоянной
раскладки записи не существует, она у каждой структуры своя. Отсюда же и
проверка `*param_2 == 0x10` в `FUN_08cd070c` — это версия схемы.

Числа при этом пишутся обычным переменным кодированием, и оно разобрано точно:

```
байт 0..251   значение — сам байт          (1 байт)
байт 252      дальше 2 байта               (всего 3)
байт 253      дальше 3 байта               (всего 4)
байт 254      дальше 4 байта               (всего 5)
```

#### Таблица-схема найдена: это блоб `TTD`

Схема лежит в блоке `TTD`, на который указывает слово +56 заголовка `Lit\x02`.
Раньше я его отверг («словарём не оказался») — и зря: он не словарь, он
**грамматика**. Читается как массив 16-разрядных слов big-endian; `openLit`
именно поэтому меняет байты местами попарно, когда порядок машины другой.

У `LIT` это 7880 байт по смещению 384 = 3940 слов:

```
заголовок  10 слов, и слово 5 = 3940 — ровно число слов в таблице
записи     по 10 слов каждая, всего 393
```

Раскладка записи выведена из `FUN_08cd0798` и сходится с данными:

```
w0  младший байт — код операции, старший — флаги
    бит 15  запись пропускается
    бит 11  запись условная (0x08 в старшем байте)
    бит 10  условие инвертируется
w2  тип поля — тот самый, что уходит в this+188
w4  переход: индекс слова, куда прыгает разбор
w7  маска, w9  значение: условие (значение & w7) == w9
```

Сходится сразу двумя независимыми проверками: слово 5 заголовка равно
фактическому числу слов, а **первая же запись имеет код операции `0x10`** —
ровно то, чего требует `FUN_08cd070c` проверкой `*param_2 == 0x10`.

Начало таблицы читается как осмысленная грамматика:

```
  0  op 10                     начало
  1  op 27  тип 0x46 'F'
  2  op 26  тип 0x29 ')'
  4  op 24  тип 0x25 '%'
  5  op 24  тип 0x26 '&'
  7  op 80  тип 0x5b '['       номер кода словаря
 14  op 44  тип 0x5d ']'       определение записи словаря
 19..39  op c0, условные: (тип & 0xf8) == 0x90…0xf0, переход на под-грамматику
```

Записи 19–39 — это **таблица разбора по типу записи**: маска `0xf8` или `0xff`,
значения `0x90`–`0xff`, и у каждой свой переход. Отсюда и байты `0x12`, `0x22`,
`0x42`, `0xb2`, `0xc2`, которые я пытался разложить по постоянному правилу.

#### Словарь прочитан, имена разворачиваются

Дизассемблер грамматики — `src/litschema.js`. Первые семнадцать записей схемы
оказались определением **словаря блока**:

```
  9  ЧИСЛО байт            прочитать байт b
 10  0x81 тип '['          если (b & 0xf0) != 0 — номер кода из старшего полубайта
 11  ЧИСЛО байт тип '['    иначе номер кода — следующий байт
 12  0x81 арг 9            если (b & 0x0f) != 0 — длина из младшего полубайта
 13  ЧИСЛО байт арг 9      иначе длина — следующий байт
 14  СТРОКА тип ']'        столько байт текста
```

Вот объяснение аномалии, на которой я застрял дважды: `0x12` — это код +1 и два
байта текста, а `0x02` — старший полубайт нулевой, поэтому номер кода задаётся
отдельным байтом и запись длиннее на байт. Никакого постоянного «тег плюс
длина» не было и быть не могло.

Читатель — `src/litdict.js`. Проверка на блоке 30685, где часть названия лежит
открытым текстом:

```
сырые байты   4341525245522044 fc01300427a0ff45 064f6082c38901d0 ...
как текст     CARRER D........
после развёртки                    ... CARRER D'ALFONSO
```

И дальше по блоку идут настоящие валенсийские названия: `CALLE DEL ALT
MAESTRAT`, `BAIX MAESTRAT`, `VINALOPÓ`, `CARRER DEL`, `CALA`. Доля букв и
пробелов после развёртки — 92.4 % против примерно 40 % у сырых байт.

**Развилка «задать/прибавить» разобрана, и правило оказалось верным.** У типа
`[` в прошивке два режима, выбор идёт по биту 12 флагов записи схемы: у записи
10 (`w0 = 0x1c81`, путь через полубайт) бит стоит — прибавить; у записи 11
(`w0 = 0x0826`, путь через байт) сброшен — задать. Это ровно то, что уже было
реализовано.

А блоки, где номер кода уходил за 255, **вообще не начинают структуру**. Их
заголовок — не заголовок: у блока 54010 это `b5 37 7c 53 40 7c 27 6d 65 7e 7c 7c 25`,
то есть кусок транскрипции `|S@|'me~||%`. Такие блоки продолжают предыдущий.
Отличить легко: у настоящего заголовка байт 3 равен `0x18` или `0x10`, а дальше
нули. Ровная проверка: **13 байт заголовка раскладываются грамматикой точно** —
записи 1–6 читают 2+1+1+4+4+1 = 13 байт, и последний из них, поле `G`, и есть
объявленное число записей словаря.

#### Машина исполняет грамматику и достаёт названия

`src/litvm.js` исполняет схему: чтения из потока, условия, переходы, вызовы,
циклы, словарь, координаты.

**Ключ ко всему нашёлся в хвосте `FUN_08cd0798` — `switch(puVar23[1])`.** Слово
`w1` записи схемы задаёт, *куда* положить прочитанное значение:

```
w1 = 0   никуда (значение уходит только в поле по типу)
w1 = 1   счётчик цикла 0xa0            кадр +36, short
w1 = 2   кадр +38, short
w1 = 3   ширина координат для 0x40
w1 = 4   регистр кадра, байт 0         кадр +32, uint
w1 = 5   тот же регистр, сдвиг 8
w1 = 6   тот же регистр, сдвиг 16
w1 = 7   тот же регистр, сдвиг 24
w1 = 8   счётчик цикла 0xa2            кадр +40, short
w1 = 9   длина строки для 0x44 и 0x43  кадр +42, short
```

Бит 12 флагов у любого из них означает «прибавить» вместо «задать». И главное:
**условия и код `0x81` смотрят на регистр (поля 4–7), а не на «последнее
значение вообще»**. Пока это было не так, словарь набирал 67 кодов из 212.

Вторая поправка того же порядка: под-грамматики вызываются переходом `0xc0`, а
не вызовом, поэтому возвращаться из них некуда — `0x11` без цели означает конец
записи и возврат к началу цикла записей, а не конец разбора. Пока машина
останавливалась на первом же `0x11`, она проходила 7 % блока; после поправки —
60 %.

**Результат: из блоков достаются настоящие названия.** На блоке 30685 машина
даёт 212 кодов словаря и 1044 записи, и в строковых полях читается
`ALT MAESTRAT`, `BAIX MAESTRAT`, `BAJO SEGURA`, `BARRANC DE MARCH`, `VINALOPÓ`,
`LLEBEIG`, `RÍO CENIA`, `GIRONA`, `CARRER DEL`, `DEPOSITO` — валенсийские
комарки и улицы, то есть разбор попадает в реальные данные, а не в шум.

**Все коды операций реализованы.** Последний неизвестный, `0x42`, нашёлся в
ветке после `0x40`: он читает блок длиной из поля `w1 = 2` (кадр +38) — тот же
приём, что у `0x43` и `0x44`, только длина берётся из другого поля. Заодно
исправлены две вещи:

* **бит 13 флагов — это «прочитать, не съедая»**: курсор возвращается на начало
  той же записи, а не на начало структуры. Пока было иначе, на блоке 982
  курсор уезжал в минус и машина плодила 341 554 пустых записи;
* **ширины `0x40` ровно как в прошивке**: 12 бит — 3 байта, 8 — 2, 16 — 4,
  24 — 6, прочие — ни одного.

Добавлена защита: виток цикла записей обязан съедать байты, иначе разбор
крутится вхолостую.

**Насколько это проверено, честно.** По 200 блокам неизвестных кодов больше нет.
В среднем прочитано **72 % блока**; до конца (в пределах ±64 байт) доходит
**62 блока из 201**. Остальные встают на «чтении за концом блока» (118), «витке
без продвижения» (61) и пределе шагов (2).

**Названия читаются.** На блоке 30685 машина даёт 212 кодов словаря и 1583
записи, и в них видны настоящие имена: `SANT JAUME BUIGUES`, `VALENCIA`,
`CATALUNYA`, `GENERAL BATISTA BASSET`, `CARLES SALVADOR`, `CARRER PAPA JOAN
XXIII`, `VENTURA COSTA`, `MIJARES`, `XUQUER`, `MONTNEGRE`.

**Поля разделились по назначению, и это важно для генерации.**

*Тип 138 — транскрипция для синтеза речи.* 233 значения, длины 37–39 и 100–124
байта, содержимое читается прямо: `ka|'mi|no||ba|'rra` — «camino barra»,
`'ka|Le||'dels||pi|ka|'pe|Drers` — «calle dels Picapedrers». Разворачивать её
словарём **нельзя**: строчные буквы здесь фонемы, а не коды, и развёртка
превращает их в `GA`, `SA`, `QU` — тот самый мусор, который я поначалу принял за
огрехи разбора.

*Типы 75 и 77 — сами названия*, сжатые словарём, со знаком `%` как
разделителем. После развёртки:

```
тип 77   %CARRER  DE L'A %A%CARRER VALENCIANA%CARRER CALECONDE%CALLE
тип 75   ÓBAL COLÓN%CALLE  DE L'DEL CARRER DE LA U L%CALLE SAN  DE L'
```

`ÓBAL COLÓN` — хвост «CRISTÓBAL COLÓN», и рядом с ним в том же блоке лежит его
транскрипция `kris|'to|for||ko|'lom`. То есть имя и произношение сходятся.

#### `0x11` оказался концом цикла `0xa2`

Разбор обрывался рано по простой причине: `0x11` — не «конец структуры», а
**конец цикла `0xa2`**. В прошивке он сверяет счётчик витков (кадр +56) с
пределом (кадр +40) и возвращается назад, и только когда витки кончились —
уходит по `w4` или поднимается на уровень выше. Я возвращал сразу.

После поправки обход вырос заметно:

```
                        было      стало
прочитано блока          72 %      87 %
блоков дочитано до конца 62/201   113/201
```

#### Названия лежат в поле типа 32

Это поле читает код `0x42` (запись схемы 105), длину берёт из поля `w1 = 2`.
Внутри — пул названий, разделённых знаком `%`. Там, где разбор попадает точно,
читается осмысленное:

```
MEDITERRANEO%AUTOPISTA  DE L'L TIO%CALLE ÁNEO%AUTOPISTA CALAOLMO%CALLE
CARRER DEL CARRER ES%AVENIDA%PLAÇA%CARRER PLAÇA
```

Заодно пришлось ограничить рост развёртки: в словаре попадаются циклы (код
раскрывается сам в себя через другой), и без предела `src/litdict.js` уходил в
бесконечность.

#### Кадры: вызов ничего не обнуляет

Подозрение на кадры декомпиляция подтвердила прямо. В обработчике `0xc1`/`0xc2`:

```
if (*(short*)(this + 156) < 3) {          // уровней не больше трёх
    ...
    *(short*)(this + 156) += 1;           // уровень вверх
    установить указатель схемы нового уровня
    param_1[уровень * 8 + 0xb] = аргумент;
}
```

Видно три вещи. **Вызов кладёт только аргумент** — остальные поля кадра
сохраняют прежнее содержимое, а я заводил чистый кадр. **Уровней не больше
трёх**, и при переполнении прошивка просто **не делает вызов**, а не падает.
И у `0xc2` аргумент — это поле регистра: сдвиг вправо на `w5 >> 8`, ширина
`w5 & 0x1f`; у `0xc1` аргумент равен самому `w5`.

После этих поправок:

```
                          было      стало
прочитано блока            87 %      93 %
блоков дочитано до конца  113/201   103/201
```

#### Бит 14 и сброс счётчиков — и названия пошли

Два последних куска нашлись в обработчике `0x10`:

```
if (param_1[2] != 0) {                  // сюда пришли переходом 0xc0
    uVar3 = puVar23[stride];            // слово 0 СЛЕДУЮЩЕЙ записи
    while ((uVar3 >> 0xe & 1) != 0) {   // пока у неё взведён бит 14
        puVar23 += stride;              // пропустить её
        uVar3 = puVar23[stride];
    }
}
*(short*)(кадр + 10) = 0;               // и сбросить счётчики цикла
```

То есть **после перехода `0xc0` подряд идущие записи с битом 14 пропускаются** —
это продолжения предыдущей ветки разбора, а не начало новой. Им помечены
22 записи схемы из 393. И `0x10` попутно **сбрасывает счётчики цикла кадра**.

Это и было главной причиной съезжающих смещений:

```
                          было      стало
поле 32, значений          13        76
из них читаемых             2        58
блоков дочитано до конца  103/201   116/201
```

#### Названия читаются

По 200 блокам достаётся 8689 названий. Видны настоящие итальянские адреса
Пьемонта:

```
PIAZZA CAMILLO BENSO DI CAVOUR      VIA GIOVANNI BATTISTA PERGOLESI
VIA REGINA MARGHERITA               VIA ALLA STAZIONE
STRADA COMUNALE                     STRADA PROVINCIALE DI
SAN MARTINO IN COLLE                ALESSANDRO MANZONI
GERMAGNANO   CASALBORGONE   VARISELLA   BORGOFRANCO   COSSATO
```

#### Границы названий: две версии отпали, осталась третья

`%` разделителем не оказался — это код словаря для частой приставки. Дальше
проверены и отвергнуты ещё две версии:

* **разделительного байта в пуле нет.** В развёрнутом пуле блока 7362 из низких
  байт выживает только `0x00`, и всего 56 раз на 1200 байт — для тысяч названий
  это не разделитель;
* **числовые поля не смещения.** Поля, которые по грубой проверке «на 100 %
  попадали в пул», при взгляде глазами оказались константами: поле 7 всегда
  равно 7, поле 85 принимает значения 0..3.

Осталась третья, и она лучше всего объясняет наблюдаемое. Развёрнутый пул
читается так:

```
GEL STALTEN LIER EREN STRASSE ST 7 ALT SSE ... CHSE ERLAU ALT RAK ALTEN UE
ENSTRASSE ST 10115 TER 35 EREN STRASSE RA CHAUSSEE HN CH 11 BR ST 30 CHSE
SCHU ÜRA 8 ENDORF IEDER STRASSE 29 BRÜCK OR TER BRÜCK BR SSE DEUTSCH HAM
```

Это **не цельные названия, а их куски**: `GEL`, `ALTEN`, `EREN`, `STRASSE`,
`BRÜCK`, `ENDORF`, `CHAUSSEE`, `DEUTSCH`. То есть сжатие в `LIT` двухслойное:
словарь биграмм на блок (разобран) и поверх него **пул составных частей
названий**, из которых имя собирается по ссылкам. Где лежат сами ссылки — пока
не найдено.

#### `PIT` прочитан целиком — это таблица стран

У `PIT` **та же самая схема**, что у `LIT`: тот же размер блока `TTD`, те же
393 записи грамматики. И на нём машина работает **без единого сбоя**:

```
717 блоков, прочитано 100 % каждого, 32 541 запись
```

Это лучшая проверка машины из всех: на `LIT` она проходит 88 % блока, на `PIT` —
весь.

Внутри — таблица стран с названиями на всех языках и произношением:

```
поле 32   AUSTRIA ÖSTERREICH AT AUT     BELGIUM BELGIË BE BEL
          SWITZERLAND SCHWEIZ CH CHE    GERMANY DEUTSCHLAND DE DEU
          FRANCE FR FRA                 IRELAND IE IRL
          ITALY ITALIA IT ITA           MONACO MC MCO
          NETHERLANDS NEDERLAND NL NLD  PORTUGAL PT PRT
          SPAIN ESPAÑA ES ESP           SWEDEN SVERIGE SE SWE
          GREAT BRITAIN GB GBR
поле 59   переводы: USTURYA, OSTENRIJK, RUOTSI, ΣΟΥΗΔΙΑ, ESPAÑA, GRÃ-BRETANHA
поле 103  произношение с меткой языка:
          de_DE 'Spa:|ni^@n     en_GB 'swIt|z@|l@nd    fr_FR 'sHis
          it_IT 'zvi|ttse|ra    nl_NL 'zwIt|s@r|%lAnt  pt_PT su|'i|s6
```

В одной из записей лежит дата набора — `10.11.2021`.

#### Таблица стран прочитана целиком: 13 записей, Кипра нет

Прежняя путаница разъяснилась. Записи стран лежат в блоках 0 и 1 и имеют
**ровно 216 байт** каждая; блоки 2 и дальше — это большие пулы по 32 КБ, и я
поначалу печатал их как «испорченные записи стран». Записей стран ровно
**тринадцать**, и каждая читается одинаково:

```
<коды> <ISO2> <ISO3> <по-английски> <на родном> <на родном> <произношение по языкам>

AT AUT AUSTRIA ÖSTERREICH          DE DEU GERMANY DEUTSCHLAND
BE BEL BELGIUM BELGIË              FR FRA FRANCE
CH CHE SWITZERLAND SCHWEIZ         IE IRL IRELAND
IT ITA ITALY ITALIA                MC MCO MONACO
NL NLD NETHERLANDS NEDERLAND       PT PRT PORTUGAL
ES ESP SPAIN ESPAÑA                SE SWE SWEDEN SVERIGE
GB GBR GREAT BRITAIN
```

**Кипра в таблице нет.** Нет и Греции, Мальты, Польши — то есть это не полный
список стран набора, а тринадцать «языковых» стран: у каждой своя метка языка
(`de_DE`, `fr_FR`, `it_IT`, `nl_NL`, `pt_PT`, `es_ES`, `sv_SE`, `en_GB`).
Похоже, `PIT` задаёт не покрытие, а **языки озвучки**.

Осторожность: хвост каждой 216-байтовой записи затекает в общий пул — там
появляется чужое произношение вроде `'wOrs|IN|tOn` (Вашингтон). Значит длина
поля берётся шире настоящей, но опознавательная часть (коды и названия) у всех
тринадцати читается целиком и однозначно.

#### Решение по объёму: адреса только по-английски

Кипрская сборка делается **только с английскими адресами**. Это снимает сразу
несколько задач:

* **новый язык в `PIT` заводить не надо** — берём готовую запись `GB` / `en_GB`;
* **переводы не нужны** — поле 59 (`USTURYA`, `OSTENRIJK`, `ΣΟΥΗΔΙΑ`) остаётся
  пустым;
* **названия остаются латиницей**, что совпадает с настоящими кипрскими
  указателями: там греческий и латинская транслитерация рядом.

#### Синтез не нужен: транскрипция необязательна

Вопрос закрыт грамматикой и подтверждён данными. Запись POI (структура `0x1002`,
записи схемы 367–386) устроена **на флагах**: почти каждое поле читается только
при своём бите флагового слова.

```
367  СТРУКТУРА 0x1002
368  ЧИСЛО байт -> регистр            флаговое слово
377  бит 0x0100        поле 0x31
378  бит 0x0200        поля 0x30 и 0x32
380  (флаги & 0x8400) == 0x0400   длина транскрипции
381  (флаги & 0x8400) == 0x0400   САМА ТРАНСКРИПЦИЯ (поле 138)
382  (флаги & 0x8400) == 0x8400   вместо неё длина 1
383  (флаги & 0x8400) == 0x8400   и пропуск одного байта
384  бит 0x0800        координаты
385  бит 0x1000        поле 0x71
386  КОНЕЦ
```

**Снят бит `0x0400` — поле транскрипции не читается вовсе.** Никакой заглушки
при этом не требуется: разбор просто идёт мимо.

И это не теория. В настоящем наборе по 200 блокам:

```
записей структуры 0x1002       7519
из них с транскрипцией         2802   37.3 %
из них БЕЗ транскрипции        4717   62.7 %
```

То есть **почти две трети точек в заводской карте уже живут без произношения**.
Головное устройство читает их каждый день. Для кипрской сборки достаточно
выставлять флаг без бита `0x0400` — и синтез фонем не нужен совсем.

Плата известна и невелика: такие названия навигация **покажет, но не
произнесёт**. Ровно как она уже поступает с большинством заводских точек.

#### Карта структур и где на самом деле лежит название

В грамматике 25 структур. По данным (200 блоков) они распределены так:

```
0x7     68 121   чистая геометрия: координаты 0x22, 0x3e, 0x3d, 0x56 — имени нет
0x30     3 665   именованный объект: геометрия в цикле + НАЗВАНИЕ
0x1002   1 535   точка интереса: флаговая запись с необязательной транскрипцией
0x33     1 187   0x31 851   0x6 489   0x32 464   0x24 341   0x12 303   0x3 211
```

**Поправка к прежнему выводу.** Я записал, что названия собираются из кусков
общего пула по ссылкам. Это неверно: **название лежит прямо в записи**
структуры `0x30`, поле `0x3b`, и читается кодом `0x44` с явной длиной.

```
284  СТРУКТУРА 0x30
285  байт -> регистр
287..292  цикл: координаты 0x66 и поле 0x24 — геометрия объекта
293  байт -> биты 16..23 регистра
294  0x81 (сдвиг 21, ширина 3) -> поле 0x35
295  2 байта -> длина          [если (регистр >> 16) & 0x1f == 0x1f]
296  0x81 (сдвиг 16, ширина 5) -> длина   [иначе]
297  СТРОКА 0x3b               НАЗВАНИЕ
300  вызов                     [бит 0x2]
301  вызов с аргументом        [бит 0x4]
302  координата 0x3c
```

То есть длина названия — **пять бит регистра** (16..20), а значение 31 служит
escape-ом на двухбайтовую длину. Экономно и объясняет, почему постоянного
правила «тег плюс длина» не существовало.

#### Названия читаются, осталось переднее кодирование

Из 200 блоков достаётся 4673 названия. Читаются целиком, например:

```
VATICAN   VATICANO   VATIKÁN   VATICÀ   ÖSTERRIKE   ØSTRIG   ØSTERRIKE
WEISSRUSSLAND   RYSSLAND   SERVIË   UNKARI   SVÉDSKO   SLOVENIA   BULGARIA
```

Но у части названий **срезано начало**: `IÉLORUSSIE`, `ECH REPUBLIC`,
`UITSLAND`, `ITSERLAND`. Это переднее кодирование, и его длина — поле `0x35`
(три бита, значения 0..7):

```
0: 2893   1: 838   2: 395   3: 214   4: 96   5: 77   6: 52   7: 108
```

У 62 % записей приставка нулевая — имя полное, и такие читаются без ошибок.

**Три варианта подстановки проверены, все три отвергнуты.** Доля читаемых имён
почти не меняется:

```
приставка от сырых байт предыдущего имени      722 из 4673   15 %
приставка от развёрнутого текста               741 из 4673   16 %
без приставки вовсе                            677 из 4673   14 %
```

Значит основа — **не непосредственно предыдущее название**. Видно и по
результату: при `p = 1` получается `USALTKRIEVIJA`, тогда как латышское
название Латвии — `LATKRIEVIJA`.

**Но само переднее кодирование подтвердилось — на конкретных примерах.** Если
посмотреть байты, лежащие в блоке перед обрезанным именем, картина складывается:

```
имя в записи     p   что рядом в блоке      целиком
USTURYA          2   VSTRIJA (AVSTRIJA)     AVUSTURYA   Австрия по-турецки
ALTKRIEVIJA      1   —                      BALTKRIEVIJA  Беларусь по-латышски
IÉLORUSSIE       1   —                      BIÉLORUSSIE   Беларусь по-французски
ITE-RUSSLAND     2   —                      WHITE-RUSSLAND
ECH REPUBLIC     2   —                      CZECH REPUBLIC
```

Всё это — названия одной страны на разных языках, идущие группой. То есть `0x35`
именно длина общей приставки, и берётся она от **соседнего имени той же
группы**.

#### Строки терялись при перезаписи — и это дало рост в двенадцать раз

Причина оказалась простой и обидной. Внутри структуры `0x30` есть цикл, и
несколько имён идут подряд, а я клал их в `rec[тип]`, где **второе затирало
первое**. Поэтому `AVSTRIJA` и не находилась: она была прочитана и тут же
потеряна.

Машина теперь ведёт строки отдельным потоком, с их смещением в блоке и длиной
приставки. Итог по тем же 200 блокам:

```
                     было      стало
строк поля 0x3b      4 673    58 631      в 12.5 раза больше
похожих на название    741    17 566
разных названий          —    13 861
```

И подстановка заработала: `AVSTRIJA` + приставка 2 даёт **`AVUSTURYA`**.

**Имена лежат по алфавиту** — 65 % соседних пар идут по возрастанию. Это и
объясняет переднее кодирование: в отсортированном списке соседи делят начало.
Видны настоящие названия со всей Европы: `ANDORRA`, `ANDORRE`, `ARNAVUTLUK`,
`ALEMANHA`, `BALTARUSIJA`, `AUSZTRIA`, `BETHMANN BANK`, `FRANZ-HIRTH-STRASSE`,
`REGINA ELENA`, `ÖLMÜHLE`, `OKELER STRASSE`.

#### Приставка верна, ошибка в развёртке словаря

Разбор участка расставил всё по местам. Последовательность строго по алфавиту:

```
ANDORA  ANDORRA  ANDORRE  ARNAVUTLUK  AUSTRIA  ÁUSTRIA  ÀUSTRIA  AUSZTRIA
p=1 "TRICHE"      -> ATRICHE       должно быть AUTRICHE
p=0 "AVSTRIJA"    -> AVSTRIJA
p=2 "USTURYA"     -> AVUSTURYA     верно
p=0 "B.JUG.R. MAKEDONIE"
p=1 "ALTARUSIJA"  -> BALTARUSIJA   верно
```

**Приставка не виновата.** У `AVUSTURYA` (приставка 2 от `AVSTRIJA`) и
`BALTARUSIJA` (приставка 1 от `B.JUG.R.…`) она сработала точно. А у `AUTRICHE`
на символ короче оказался **сам суффикс**: развернулось `TRICHE` там, где должно
быть `UTRICHE`.

**Поправка: словарь тоже не виноват.** Сырые байты `TRICHE` — это
`54 52 31 48 45`, то есть `T`, `R`, код `0x31` = `IC`, `H`, `E`. Развёртка
верна, суффикс действительно `TRICHE`, значит приставка должна быть 2, а
записано 1.

#### Приставка считается в БАЙТАХ сжатой формы

Разгадка нашлась при взгляде на байты вокруг имени:

```
2576  00 25 54 52 31 48 45 00
       ^  ^  <--- строка @2578, длина 5 --->
       |  байт 0x25 = 0b001_00101: приставка 1, длина 5
       разделитель
```

Приставка равна 1, а нужные буквы — «AU». Предыдущее имя `AUSZTRIA` в сыром
виде — `f5 be 54 cd`, и **первый его байт `f5` это код «AU»**. То есть приставка
отсчитывается не в символах текста, а **в байтах сжатой формы**: один байт может
раскрыться в два символа.

Проверка по оракулу отсортированности не оставляет сомнений:

```
приставка в символах   64.5 % возрастающих пар
приставка в байтах     75.9 %
```

И список стал читаться подряд:

```
ALBANIA ALBÁNIA ALBÂNIA ALBANIE ALBÁNIE ALBANIEN ALBANIJA ALBANIË ALBÁNSKO
ALEMANHA ALEMANIA ALEMANYA ALLEMAGNE ALMANYA ALVANIA ANDORA ANDORRA ANDORRE
```

Албания и Германия на всех европейских языках, строго по алфавиту. До поправки
на их месте стояли `AANIEN`, `AÁNSKO`, `ALEMAGNE`, `AMANYA`.

Байт перед строкой упакован так: **младшие 5 бит — длина** (31 = escape на
двухбайтовую), **старшие 3 бита — приставка**.

Скачок на семёрке в распределении приставки оказался **не escape, а потолком**:
такие записи читаются не хуже прочих. Доля возрастающих пар по значению
приставки:

```
0: 68.4 %   1: 79.8 %   2: 80.0 %   3: 78.8 %
4: 80.9 %   5: 82.4 %   6: 84.0 %   7: 82.8 %
```

Семёрка держится наравне с шестёркой, значит кодировщик просто не делит больше
семи байт. Низкая доля у нуля ожидаема: такие записи начинают новую цепочку, и
предыдущее имя им не родня.

#### Читатель `src/litnames.js`

Складывает все четыре слоя и отдаёт названия в файловом порядке.

```
node src/litnames.js lit 60     12 234 разных названия с 61 блока
node src/litnames.js pit        141 455 разных названий
```

Проверка глазами — `AUTRICHE` собирается правильно, а список идёт подряд:

```
A. R. Y. MACEDONIA  AGIOS MARINOS  ALANKOMAAT  ALBANIA  ALBÁNIA  ALBÂNIA
ALBANIE  ALBÁNIE  ALBANIEN  ALBANIJA  ALBANIË  ALBÁNSKO  ALEMANHA  ALEMANIA
ALEMANYA  ALLEMAGNE  ALMANYA  ALVANIA  ANDORA  ANDORRA  ANDORRE  ARNAVUTLUK
AUSTRIA  ÁUSTRIA  ÀUSTRIA  AUSZTRIA  AUTRICHE
```

#### Насколько полно читается — честные цифры

**Оракул отсортированности оказался негодной мерой.** Он давал 88 % и внушал
ложный оптимизм: почти все «нарушения порядка» при разборе оказались **верными
названиями**, просто база сортирует не так, как любой из проверенных
компараторов:

```
ALBÂNIA -> ALBANIE     ÁUSTRIA -> ÀUSTRIA     BÉLGICA -> BÈLGICA
ESLOVÁQUIA -> ESLOVÀQUIA        ÉSZTORSZÁG -> EX REP. JUG. MACED.
```

Все четыре способа сравнения (локальный, по кодовым точкам, по байтам UTF-8,
по байтам latin1) дают около 88 % и спотыкаются на разных парах — значит у базы
своя таблица сортировки, и мерить ею качество нельзя.

**Прямая мера — порча текста.** По 327 089 извлечённым названиям:

```
пустых              1 848    0.6 %
с упр. символами  207 407   63.4 %
с битым UTF-8     214 755   65.7 %
ЧИСТЫХ            106 312   32.5 %
```

Остальное — вот такой мусор:

```
"669436384936476570290631372657384937/26P"
"3134�4933465945737185525636VDER5856"
"9780868747151·78687458572929185549472484"
```

Сплошные цифры: машина принимает за строки числовые поля. Значит смещения
внутри записи всё ещё уезжают, и поле `0x44` срабатывает там, где строки нет.

Для сравнения: у `PIT` того же формата разбор идёт на 100 % блока, и мусора
такого нет.

#### Структура занимает несколько блоков каталога

Заголовок со словарём есть только у **первого блока структуры**, остальные —
продолжения, и поблочно они разбираются с пустым словарём. Карта по каталогу
показывает, где это важно: первая четверть заголовочная целиком, дальше
заголовки редеют — на участке 225 058 их всего 2 из 30.

```
участок каталога   заголовки   прочитано   до конца
       0..            30/30        97 %      26/30
   20459..            30/30       100 %      30/30
   61379..            14/30        94 %      15/30
  102299..             8/30        90 %      17/30
  225058..             2/30        97 %      18/30
```

Поэтому читатель склеивает группу: заголовочный блок плюс следующие за ним
продолжения.

#### Пересинхронизация проверена и отвергнута

Склейка теряет имена: машина встаёт посреди большого буфера и больше не
стартует. Напрашивалось продолжать разбор с места остановки с уже набранным
словарём — и это добавляло сотни тысяч названий. Но мера по порче показала, что
все они мусорные:

```
режим                      имён     чистых
по блокам, один прогон   241 085   105 288   43.7 %
группами, один прогон    198 732   105 023   52.8 %
группами + пересинхр.    327 089   106 312   32.5 %
```

**Число чистых названий во всех трёх режимах одно и то же — около 105 тысяч.**
Пересинхронизация добавила 128 тысяч записей и ни одного чистого названия.
Значит потолок задаёт сама машина, а не число проходов, и из читателя приём
убран.

За основу взят средний режим: склейка группы, один прогон.

**И ещё одна поправка к себе: «чистых 52.8 %» — завышено.** Проверка «нет
управляющих символов» пропускает цифровую кашу вроде `86444` или `822923ERELCH`.
Строже:

```
всего извлечено          198 732
нет упр. символов        105 023   52.8 %
+ букв больше половины    85 207   42.9 %
+ похоже на название      66 266   33.3 %   <- честная цифра
```

Разных правдоподобных названий — **50 905**.

**Ошибка систематическая, а не накапливающаяся.** Из 213 групп с именами: 66
читаются целиком чисто, 25 целиком мусор, 114 вперемешку и лишь 8 ломаются и
не чинятся. Значит дело не в дрейфе позиции, а в конкретных ветках грамматики,
куда разбор заходит ошибочно.

Вход в структуру `0x30` один-единственный — запись 21 диспетчера, по условию
`(байт типа & 0xf8) == 0xd0`. Таблица диспетчера покрывает не все байты:
`0x81`–`0x8f`, `0x9a`–`0x9f`, `0xc0`–`0xc7` и `0xf8`–`0xff` ни под одно условие
не подходят и проваливаются на `0xc3`. Возможно, там и теряется синхронизация.

Ещё одна привязка: все названия приходят из **одного места** — структура `0x30`,
запись схемы 297. Поблочно чистых там 69 %, значит портится не чтение строки, а
попадание в неё.

#### `0xc3` — это ошибка, а не выход. Точность выросла вдвое

Проверено по декомпиляции: в `FUN_08cd0798` у кода `0xc3` **ни одна ветка не
выставляет код возврата**, и тот остаётся `-5`, выставленным перед switch. А в
`FUN_08ccf84c` обработчика для `-5` нет вовсе — он проваливается в `default`.
Значит `0xc3` означает **ошибку разбора**, и в исправных данных не встречается.

Я же трактовал его как аккуратный выход из под-грамматики, и разбор после
рассинхронизации продолжал грызть мусор. После замены на жёсткий останов:

```
                        было        стало
извлечено             198 732     112 214
похожих на название    66 266      63 792
доля                    33.3 %      56.8 %
разных названий         50 905      48 884
```

Мусора стало 48 тысяч вместо 132, при потере всего 4 % годных названий.

#### Где теряется синхронизация

Теперь видно точно. Из 342 групп 242 останавливаются рассинхронизацией, и
последняя разобранная структура перед ней:

```
108  структура 0x7    (запись 343)   чистая геометрия
 67  структура 0x1002 (запись 367)   точка интереса
 13  структура 0x6     12  0x3     7  0x24     5  0x1001
```

Виновата прежде всего `0x7` — самая частая структура (68 121 запись) и самая
простая на вид. Она читает три упакованные пары координат кодом `0x40`, ширину
которых задаёт байт 3 заголовка блока. Ширина проверена и верна: в заголовках
лежит 24 (0x18) или 16 (0x10), обе обрабатываются.

Отдельно примечательно, что `0x7` **вызывает** `0x1002` (запись 348, при бите
`0x10`) — то есть две главные причины рассинхронизации связаны в цепочку.

#### Трасса срыва в структуре `0x7`

Снята трасса последних сорока шагов перед рассинхронизацией:

```
351  a2  @3806     начало цикла, счётчик 2
352  41  @3806     дельта-кодек съел байт 0x73
355  11  @3807     виток
352  41  @3807     съел байт 0x02
355  11  @3808     цикл кончился
 17  10  @3808     возврат к началу записей
 18  26  @3808     читает байт типа -> 0x83
 19..40 c0 @3809   ни одно условие не подошло
 41  c3            срыв
```

Байты вокруг: `… 1d 73 02 83 02 09 34 06 …`. Прочитанный как тип байт `0x83`
попадает в непокрытый диапазон `0x81`–`0x8f`. А если бы цикл сделал **три**
витка, типом оказался бы следующий байт `0x02` — он подходит под запись 19
(`байт & 0x80 == 0`) и разбор пошёл бы дальше. То есть не хватает ровно одного
байта.

Длинная трасса показывает запись целиком:

```
 17 10 @3799                новая запись
 18 26 @3799  съел 1 б      байт типа 0x0a
 19 c0 @3800                (0x0a & 0x80) == 0 -> структура 0x7
344 40 @3800  съел 6 б      упакованная пара, ширина 24
345 81 @3806                (reg & 3) = 2
346 40, 347 40              условие (reg & 3) == 3 — не сработало
348 c1                      условие (reg & 0x10) — не сработало
350 81 @3806                счётчик цикла = (reg >> 2) & 3 = 2
352 41 @3806  съел 1 б      байт 0x73, положительный
352 41 @3807  съел 1 б      байт 0x02, положительный
 17 10 @3808                байт типа 0x83 — срыв
```

Итого запись съела 1 + 6 + 2 = **девять байт, а нужно десять**.

**Четыре гипотезы проверены и отвергнуты.** Все обваливают разбор:

```
исходное                        112 214 названий   56.8 %
счётчик цикла + 1                 4 124            59.4 %
без пропуска по биту 14             763            39.7 %
счётчик из 0x81 + 1               4 706            57.5 %
```

Точность у них чуть выше только потому, что разбор обрывается почти сразу.

Отдельно сверен построчно **дельта-кодек `0x41`** — все восемь его ветвей
(`0xff`, положительный байт, `m = 0x00 / 0x20 / 0x40 / 0x60` с подслучаями
`v = 0xc / 0xd / 0xe / прочее`) считают байты ровно как в прошивке. В этой
трассе оба входа положительные и съедают по байту — верно.

Ширина координат тоже вне подозрений: поле 3 пишет **только** запись 3, то есть
байт 3 заголовка блока, и он равен 24 или 16.

Заметка на будущее: у записи 343 взведён бит 14, то есть при входе переходом
она **пропускается**, и байт типа из диспетчера служит структуре флагами. Это
объясняет, почему все условия внутри `0x7` смотрят на один и тот же регистр.

#### Слой выше разборщика: итератор

Раскопки в прошивке открыли слой, которого в моей машине нет вовсе. Над
`FUN_08ccf84c` сидит **шаг итератора** `FUN_08cc8800`, и именно он решает, где
кончается элемент. Он вызывает разбор в цикле и смотрит на код возврата:

```
if (код < -4 || (короткое поле < 1 && код < -1)) -> остановка
```

То есть **только `-5` безусловно останавливает** разбор, а `-2`, `-3`, `-4` —
рабочие состояния, итератор продолжает. Это подтверждает решение считать `0xc3`
(который даёт `-5`) жёстким остановом.

Дальше итератор раскладывает по **типу структуры**, и это ровно наши номера:
`0x18`, `0x26`, `0x30`, `0x31`, `0x32`, `0x34`, `0x80`. Признак начала структуры
— значение `0x52` в поле статуса при коде возврата 2, а сам тип лежит по
смещению `+0xa0`.

#### Что такое запись POI — из потребителя

`CLitPoiTile::CLitPoiTile` (`FUN_08cdeee4`) ходит по элементам тайла:

```
do { если шаг вернул 0 -> "POI %u (%u) not found" }
while (короткое поле >= 0 && тип записи != 0x32);
если тип записи != 0x24 -> "Element %u is not a POI (index %u, type %d)"
```

Отсюда прямо: **структура `0x24` — это и есть точка интереса**, а `0x32`
ограничивает перебор. Тип записи лежит по смещению **1068** в объекте
разборщика — то же смещение, что мы нашли раньше в `FUN_08cdf1e8`, где
проверяется `ISLit_POITile == 0x23`.

Инструмент для таких раскопок: `scratchpad/gs/LitOne.java` — headless-скрипт,
который дизассемблирует диапазон, **создаёт функцию по точному адресу** и
декомпилирует её. Нужен потому, что анализ по диапазону в некоторых областях
функций не создаёт (на `0x08cc8000`–`0x08cca000` дал ноль).

Разобрано в итераторе:

```
поле 1568   пока ноль — вызывается 0x08cc7b3c, сброс состояния под тип
            структуры (ветки на 0x20, 0x7, 0x1, 0x8)
поле  156   уровень вложенности
поле  188   что разобрано: w2 поля, либо 0x52 — «начало структуры»
поле  160   значение; при 0x52 это номер типа структуры
0x08ccf84c  шаг разбора — та самая FUN_08ccf84c, которую моделирует машина
```

**Где упёрлись.** Дальше итератор раскладывает по типу структуры через `braf`, и
декомпилятор эти таблицы не восстанавливает («Could not recover jumptable»).
Цели посчитаны вручную — `0x08cca4ca`, `0x08cca5aa`, `0x08cca692`, `0x08ccaab8`,
`0x08ccaad4`, `0x08ccaafe`, `0x08ccab02` — но обработчик по умолчанию (куда идёт
наша структура `0x7`) оказался всего лишь разбором кодов возврата `-2` и `-4`,
без работы с указателем данных.

Итог раскопок: слой итератора понят, но **недостающий байт в нём не нашёлся**.
Байты считает только `FUN_08ccf84c`, и она смоделирована.

#### Сплошная сверка разборщика с прошивкой

Раз одиночная гипотеза не находилась, я сверил разборщик целиком.

**Все коды операций реализованы.** В схеме встречается 24 разных кода, и для
каждого в машине есть ветка — пропущенных нет:

```
0x10:27  0x11:27  0x24:2  0x25:41  0x26:53  0x27:21  0x29:13  0x40:28
0x41:69  0x42:1   0x43:9  0x44:6   0x60:1   0x63:1   0x65:4   0x80:11
0x81:26  0xa0:5   0xa1:5  0xa2:12  0xc0:22  0xc1:5   0xc2:3   0xc3:1
```

**Дельта-кодек `0x41` сверен исчерпывающе.** Я выписал эталонную таблицу расхода
байт прямо из декомпиляции и прогнал свою реализацию по **всем 256** значениям
первого байта: расхождений **0 из 256**. То же для `0x25` (переменная длина,
включая тонкость: `0xff` съедает один байт и даёт значение 255) и `0x40`
(ширины 8, 12, 16, 24 и «прочее — ни одного байта»).

#### Промах не в одну сторону

Решающее измерение: на каждом срыве я перебирал сдвиг от −3 до +6 и брал тот,
после которого разбор идёт дольше всего. Если бы не хватало одного байта,
распределение упёрлось бы в `+1`. Оно другое:

```
-3 :  35  #########
-2 :  48  ############
+1 :  30  #######
+2 :  19  #####
+3 :  17  ####
+4 :  15  ####
+5 :  17  ####
+6 :  20  #####
```

Отрицательные сдвиги (перебор байт) встречаются **чаще** положительных. Значит
единого «недостающего байта» нет: ошибки разные и возникают на разборе
структур переменной длины.

**Пересинхронизация со сдвигом проверена.** Даёт +2022 годных названия
(63 792 -> 65 814), но добавляет 87 тысяч мусорных: точность падает с 56.8 % до
33.0 %. В читателе не используется.

**Вывод.** Статический разбор прошивки по этому вопросу исчерпан: каждая ветка
сверена, расход байт совпадает. Потолок чистых названий — около 64–66 тысяч при
любом способе, то есть упирается не в число проходов и не в отдельный код
операции, а в разбор структур, для которого нужна внешняя опора: либо дамп
памяти с живой машины, либо перебор с проверкой по алфавитному порядку.

#### Зацепка: что `PIT` не проверяет

`PIT` читается на 100 % **той же грамматикой**, значит ломаются ветки, которых в
нём нет. Сравнение исполненных кодов операций даёт список подозреваемых:

```
код    в LIT    в PIT
0x40    2773        9     <- почти не проверен
0xa1     341        0     <- не проверен вовсе
0x80     479        0
0x63      50        0
0xa0      11        0
0x65      10        0
0x24       8        0
```

#### Циклы были написаны неверно — и это стоило четверти названий

Перечитал `0xa0`, `0xa1` и `0xa2` в прошивке и нашёл два расхождения.

**Первое.** При нулевом счётчике прошивка ищет закрывающую запись и продолжает
**со следующей за ней** (`puVar12 = puVar23`, а правило берётся как
`puVar12 + stride`). Я же исполнял саму закрывающую запись — а она трогает
внешний цикл. Одна эта поправка подняла точность с 56.8 % до 62.5 %.

**Второе.** Счётчики витков и указатели циклов в прошивке лежат **в кадре**
(смещения +52, +56 и +58), а не на стеке, и `0x10` их сбрасывает. Циклов два
независимых: `0xa0`…`0xa1` и `0xa2`…`0x11`, у каждого свой счётчик. Коды `0x63`
и `0x64` читают именно их. Я держал общий стек и для `0x63`/`0x64` отдавал
верхний элемент — при вложении это давало не тот номер витка.

После переписывания циклов точно по прошивке:

```
                     имён    годных   точность   разных
было               112 214   63 792     56.8 %   48 884
пропуск закрывающей 101 120   63 245     62.5 %   48 499
циклы на кадре     122 917   78 045     63.5 %   61 222
```

#### Ещё две поправки по прошивке

**Маска разрядности сбрасывается в ноль на каждой записи** (`local_34 = 0` в
начале итерации), и задаёт её сам код операции. Нулевая маска означает, что
запись в регистр не меняет ничего. Выписаны точные значения: `0x26` → 255,
`0x27` → 0xffff, `0x28` → 0xffffff, `0x60` → 255, `0x61`/`0x63`/`0x64` → 0xffff,
`0x81` → `(1 << w5) - 1`. У меня по умолчанию стояло 0xffffffff. На данных
затронута одна запись (360, код `0x60`), так что эффект в пределах шума, но
теперь верно.

**`0x11` без цели — это конец разбора, а не возврат к началу цикла записей.**
В прошивке такая запись на нулевом уровне возвращает `-4`, а итератор на нём
останавливается (условие `уровень < 1 && код < -1`). Я же прыгал к началу цикла.
Таких записей в схеме пять из двадцати семи.

#### Итог заходов по прошивке

```
                          имён    годных   точность   разных
исходное                112 214   63 792     56.8 %   48 884
пропуск закрывающей     101 120   63 245     62.5 %   48 499
циклы на кадре          122 917   78 045     63.5 %   61 222
маски по прошивке       123 618   78 041     63.1 %   61 220
0x11 без цели = конец   121 376   77 985     64.3 %   61 165
```

Годных названий стало **на 22 % больше**, разных — на 25 %, точность выросла на
семь с половиной пунктов. И `PIT` вырос заодно: разных названий 141 455 ->
**406 915**.

Догадка про «недостающий байт» была ложной. Дело было в управлении потоком —
в циклах, — а расход байт у каждого кода и правда совпадал с прошивкой.

#### Мера качества была неверной — читается 97 %

Несколько заходов подряд я мерил качество проверкой «начинается с заглавной
латинской буквы и состоит в основном из букв». Она браковала **правильно
прочитанные данные**:

* арабские названия стран — `ألبانيا` (Албания), `إسبانيا` (Испания),
  `الفاتيكان` (Ватикан), `اليونان` (Греция), `بريطانيا العظمى` (Великобритания);
* греческие и кириллические названия;
* номера домов и почтовые индексы — `56414`, `47533`.

При проверке, не зависящей от письменности (нет управляющих символов и битого
UTF-8, доля букв больше 0.6 либо это короткое число):

```
всего извлечено   121 376
испорченных         2 639    2.2 %
ГОДНЫХ            117 725   97.0 %
разных годных      85 630
```

По письменностям: латиница 99 556, кириллица 119, арабица 92, греческий 90.

Выборка по всему объёму читается как настоящий адресный справочник Европы:

```
RIESWEILER   PFARRGASSE   SALZMARKT   AN DER SANKT VEITSMÜHLE
VIALE DUCA DEGLI ABRUZZI   VIA DOTTOR ANGELO RIBERI   VIALE SANDRO PERTINI
CALLE DE LA TORRE   CALLE MAESTRO DOLORES   SECTOR G-PARDA BENICOLADA
BUCKLAND ROAD   MILL LANE   MERE STREET   LOXTON COURT   ALDI, HINCKLEY ROAD
KUBE HOTEL PARIS   LE PETIT MARCHÉ DE RIQUET   RUE ÉTIENNE MARCEL
DÉLICES ET GOURMANDISES   CENTRE SPORTIF MOUCHOTTE
```

**Урок.** Прежние цифры (33 %, 56.8 %, 63.5 %, 64.3 %) отражали изъян меры, а не
состояние разбора. Правки по прошивке всё равно были нужны — они подняли число
годных названий с 63 792 до 117 725, — но объявлять долю до того, как мера
проверена, было ошибкой.

#### Оставшиеся 2.2 %: что проверено

Испорченные названия двух видов. Цифровая каша — разбор попал в числовое поле:

```
EN239374957TZ39965563573749RO09388389746913857394924
```

И слипшиеся названия — границы потеряны, несколько имён сшиты в одно:

```
ENERÖRTH AM RHEIN··ERSEIENWEI AN DER DONAUENEIELLEAU
ENBERGBUETTELER WEG·UR AN DER ANVORDNEUBÜTTELER WEGB
```

Куски здесь настоящие (`WÖRTH AM RHEIN`, `AN DER DONAU`, `NEUBÜTTELER WEG`), но
сшиты неверно.

Срывы дают две цепочки: `0x7` (73 случая) и `0x1002` (59). По обеим снята
трасса; в обеих запись съедает на байт меньше, чем нужно, чтобы следующий байт
типа попал в покрытый диапазон.

**Проверено и отвергнуто:**

* счётчик цикла `+1` — теперь уже после починки циклов: разбор обваливается со
  121 376 названий до 5 211;
* срывы **не** приходятся на стык блоков внутри склеенной группы: у границ
  ноль случаев, все 8 — вдали от неё (и ещё 9 в группах из одного блока);
* структура `0x1001` прослежена целиком: она читает аргумент вызова кодом
  `0x60`, при значении 1 вызывает `0x1002`, при 2 читает счётчик цикла. Расход
  байт у неё нулевой, вся работа в вызываемой.

Всё, что поддаётся сверке с прошивкой статически, сверено: расход байт у каждого
кода, маски, управление циклами, вызовы с аргументом-полем, слой итератора.

#### Чтения на 97 % НЕ достаточно, чтобы собрать файл

Важно не путать два разных умения. Разобрано то, что нужно для **чтения**:

```
контейнер и каталог      сходится с двух концов, 245 518 блоков
грамматика в блоке TTD   машина исполняет 24 кода операций
заголовок блока          13 байт, разложен по грамматике
словарь биграмм          читается
кодирование имён         алфавит + переднее кодирование + словарь
структуры записей        25 штук, поля по типам
```

Для **записи** этого мало. Не разобрано устройство самой базы точек интереса —
а оно есть в прошивке и ни разу нами не тронуто:

```
ISLit_POIMainHeader              главный заголовок базы POI
CLitPoiTileDb(numberOfPoiBlocks, numberOfSubDirs,
              numberOfLayers, numberOfTypes, poiMainHeader)
CLitPoiTileSubDirectory          под-каталоги тайлов
getTilesInRect / getTilesInTree  пространственный поиск по дереву
getTypesOfLayer / getStylesOfLayer / getMaxScaleOfType / getBoundRectOfLayer
```

То есть неизвестно: как точки разложены по тайлам, как устроено дерево для
поиска по прямоугольнику, откуда берутся слои, типы, стили и масштабы
отображения, и что лежит в главном заголовке.

**Что это значит для Кипра.** Сгенерировать запись POI мы уже можем — формат
записи, флаги и словарь известны. Но положить её так, чтобы головное устройство
нашло её на карте, пока нельзя: не хватает раскладки по тайлам и дерева поиска.

Оставшиеся 2.2 % испорченных при этом на генерацию не влияют — свои данные мы
пишем сами и в непонятые ветки не заходим.

#### Что осталось

* сузить длину 216-байтовой записи страны: хвост затекает в общий пул;
* выяснить, где задано покрытие набора, если `PIT` задаёт только языки озвучки;
* дочитывать блок до конца: сейчас 116 из 201, а 118 блоков упираются в
  «чтение за концом»;
* устройство базы POI для ЗАПИСИ: главный заголовок (`ISLit_POIMainHeader`),
  под-каталоги тайлов, дерево пространственного поиска, слои, типы и стили;
* оставшиеся 2.2 % испорченных названий (на генерацию не влияют);
* разделитель `%` внутри некоторых имён (`PARE ARNAU%CARRER`);
* найти недостающий байт в структуре `0x7`. Запись съедает девять байт вместо
  десяти. Проверены и отвергнуты: счётчик цикла +1, отказ от пропуска по биту
  14, счётчик из `0x81` +1. Сверены и признаны верными: дельта-кодек `0x41`
  (все восемь ветвей) и ширина координат;
* сузить длину у типа 59: он захватывает имя вместе с транскрипцией;
* сверить дельта-кодек `0x41` с реальными координатами — он написан по
  декомпиляции и на данных не проверен.

Коды операций, выведенные из `FUN_08cd0798`:

```
0x10 структура      0x11 конец/возврат
0x20 знаковое переменной длины      0x25 беззнаковое переменной длины
0x21/0x22/0x23/0x24 знаковые 1/2/3/4 байта
0x26/0x27/0x28/0x29 беззнаковые 1/2/3/4 байта
0x40 упакованная пара координат     0x41 дельта-кодек координат
0x43 пропуск блока   0x44 строка    0x80 константа   0x81 поле из полубайта
0xa0/0xa1/0xa2 цикл  0xc0 переход   0xc1/0xc2 вызов
``` Кодов операций в
`FUN_08cd0798` десятки, нужны не все — достаточно тех, что встречаются в наших
блоках. Блок начинается с
13-байтового заголовка (у блока 0 это `0d fa 00 18 00 …00 9f`, где `0d` = длина
заголовка, а `9f` = 159 похоже на число кодов), дальше идут записи с однобайтовым
тегом, у которого младший полубайт совпадает с длиной: `0x12` — два байта,
`0x13` — три. Содержимое явно словарное: `AN IA EN IN AR AL OV OR ER ON IT ST KO
AT RE CE RO CH SL IE LG US EG RA BR BO` плюс UTF-8 — греческие, кириллица,
арабица, `Á`, `É`. В потоке записи начинаются байтами 0x12, 0x13, 0x22, 0x32, 0x42, 0x02,
0xb2, 0xc2, а разборщик сравнивает уже 32-разрядный тип (91, 93, а также 37 `%`,
38 `&`, 41 `)`, 70 `F`).

**Проверенные и отвергнутые гипотезы.** (1) Таблицы перевода тега в тип в
образе нет: поиск по всему файлу массивов u32, u16 и байт, где рядом лежали бы
91, 93, 37, 38, — ноль попаданий. (2) «Младший полубайт равен длине» неверно:
у `0x12` полезных байт два, у `0x13` три, но у `0x02` тоже три. На блоке 0
разбор по этому правилу спотыкается на 59-й записи. (3) Совпадение «число
записей равно байту 12 заголовка» держится лишь на 5 блоках из 201 — случайность.

Что найдено по коду: конструктор разборщика — `0x08ccf780`, он обнуляет
счётчик кодов (`this+250`) и заводит указатель на пул; инициализация итератора —
`FUN_08cd070c`, она требует `*param_2 == 0x10`. Сам побайтовый разбор заголовка
записи пока не локализован — Ghidra в этой области функций не создала, а поиск
по записям в поле типа даёт ложные срабатывания (там пишется флаг, не тип). Блок `TTD` (7880 байт по смещению 384) словарём **не**
оказался: он ложится в объект по смещению 0x38, а не в 250/252/508/204.

## База точек интереса: слои, дерево, тайлы

Раскладка POI бралась не из догадок: грамматика в блоке `TTD` **объявляет** её
сама, а прошивка показывает, как по ней ходить.

### Четыре типа записей

В грамматике 27 структур; к точкам интереса относятся четыре, идущие подряд:

| тип | запись грамматики | что это |
|---|---|---|
| `0x21` | 119…126 | заголовок слоя |
| `0x22` | 127…139 | узел дерева (`ISLit_POIMainHeader`) |
| `0x23` | 140…143 | тайл (`ISLit_POITile`) |
| `0x24` | 216…242 | сама точка интереса |

### Как прошивка ходит по ним

`FUN_08cddd1c` — вход в слой:

```
позиция = массив_слоёв + номер_слоя * 0x24 + 0xc
перейти по позиции; тип записи обязан быть 0x21  («Did not find layer %u header»)
из заголовка слоя взять три позиции
перейти по первой; если тип 0x22 — обход дерева, если 0x23 — это сразу тайл
```

Массив слоёв держится в объекте по смещению `+0x60`, запись слоя занимает
**0x24 байта**, число слоёв — в `+0x5c`. Строка проверки так и звучит:
`layerIndex < mNumberOfLayers`.

`FUN_08cdd9e4` (`getTilesInTree`) — обход дерева. На каждом узле:

* берёт рамку и сравнивает с запрошенным прямоугольником по четырём границам;
* берёт до **четырёх** ссылок на потомков — это квадродерево;
* по подходящей ссылке переходит и повторяет.

При промахе печатает `Did not find POI tree branch tile`, при неверном типе
листа — `POI tile expected`.

### Позиция — это дельта-кодированная пара

Ссылки на потомков и на тайлы прошивка копирует **по 6 байт**
(`memcpy(&local_5c, &local_54, 6)`), а в грамматике на их месте стоит
`КООРД дельта-кодек`. То есть позиция в адресном пространстве `LIT` хранится
тем же кодеком, что и координаты, и занимает шесть байт.

Это видно и в заголовке слоя:

```
0x21: u16 (тип 0x4c), u32 (тип 0x41), ОП65 (тип 0x5f),
      дельта-пара (тип 0x62), дельта-пара (тип 0x2b)
```

и в узле дерева:

```
0x22: байт, пара 0x61, пара 0x60, дельта 0x5f,
      пара 0x64, пара 0x63, дельта 0x62,
      дельта 0x79, число 0x42, дельта 0x7a, число 0x7b
```

Три группы «пара + пара + дельта» — это рамка и ссылки.

### Тайл — это одно число

```
0x23: байт, беззнаковое переменной длины (тип 0x42)
```

Больше в записи тайла ничего нет. Значит тайл не хранит список точек, а лишь
задаёт, откуда их читать.

### Проверка по данным

Записи этих типов нашлись там, где и ожидалось, — **в хвосте каталога**.
Плотный проход по последним 3000 блокам из 245 518 дал 172 блока с записями
`0x21`, `0x22` и `0x23`, начиная примерно с блока 242 520. В первых 3000 блоков
их нет вовсе — там встречаются только записи `0x24`.

### Чего ещё нет

Найдено расположение, но не прочитаны сами значения: рамки узлов, ссылки и
число в записи тайла мы пока не достаём — машина грамматики в этих блоках
даёт одну-две записи на блок и, похоже, теряет синхронизацию. Пока это не
починено, дерево не обойти.

## Исправление машины: координаты затирали друг друга

Первая же попытка прочитать узел дерева показала баг, который жил в
`src/litvm.js` с самого начала и до сих пор не мешал только потому, что имена
читаются из строк, а не из полей записи.

Все три кода операций, читающие координаты (`0x40` упакованная пара и `0x41`
дельта-кодек в двух ветках), клали результат в **один ключ** `rec['xy']`.
Числа при этом кладутся в поле своего типа: `rec[type] = val`.

Для структуры с одной координатой разницы нет. А у узла дерева `0x22`
координатных полей **восемь** — и выживало только последнее. Узел выглядел
так:

```
тип 0x22   0x42=14  0x7b=2  0xNaN=[99255,26972]
```

Ключ `0xNaN` — это и есть `rec['xy']`, вылезший при печати полей по номерам.

После исправления тот же узел читается целиком:

```
тип 0x22
   0x42=14
   0x5f=[99245,4381]     0x60=[0,0]    0x61=[0,0]
   0x62=[99245,4382]     0x63=[0,0]    0x64=[0,0]
   0x79=[99245,4443]
   0x7a=[99255,26972]
   0x7b=2
```

Четыре дельта-поля дают четыре пары — ровно столько ссылок на потомков берёт
`getTilesInTree`, и вид у них правильный: прошивка копирует под позицию шесть
байт, то есть `u32` плюс `u16`, а здесь первое число около 99 245 (номер блока
в каталоге из 245 518) и второе — небольшое смещение.

Проверено, что чтение имён не пострадало: на выборке в 61 блок 512 205 имён,
чистых 96.4 %.

## Что ещё не читается

**Упакованные пары `0x60`, `0x61`, `0x63`, `0x64` дают `[0,0]`.** По коду
`getTilesInTree` рамка узла копируется отдельным куском в 0x18 байт, и,
похоже, это как раз они. Пока рамки нет, дерево не обойти по прямоугольнику.

**Ширина координат в коде `0x40` берётся из данных и часто выходит мусорной.**
Замер по 200 склейкам: 18 621 случай, когда ширина не равна 8, 12, 16 или 24.
Самые частые — структура `0x20` поле `0x3e` ширина 123 (4120 раз), структура
`0x24` поле `0x2c` ширина 144 (1072 раза), структура `0x7` поле `0x7c`
ширины 41, 0, 67, 245. Значения выглядят как байты потока, а не как ширина,
то есть к этому месту машина уже разошлась с данными.

Это следующая цель: пока ширина берётся неверно, ни рамки узлов, ни списки
точек в тайле прочитать нельзя.

## Ширина координат берётся из состояния разбора

В коде `0x40` (упакованная пара) ширина берётся так:

```
piVar24 = (int *)param_1[1];      // состояние разбора
...
sVar9 = (short)piVar24[3];        // поле +0xc этого состояния
if (sVar9 == 0xc) ... else if (sVar9 == 8) ... else if (sVar9 == 16) ...
```

То есть ширина — **не поле правила** и не число, прочитанное тут же, а
состояние, которое живёт между записями и меняется отдельными правилами.
Проверено: у всех правил `0x40`, относящихся к POI (129, 130, 132, 133, 219,
234, 84, 85), слова 5…9 нулевые, так что из правила её взять неоткуда.

Наша машина это уже моделирует — `wid` меняется в `store` при `w1 == 3`.
Отсюда важное следствие: **любая рассинхронизация портит ширину надолго**, и
мусорные значения 123, 144, 41, 245 — это симптом, а не причина.

## Позиция в LIT — это (номер блока, ключ записи), а не смещение

Переход по ссылке идёт через `FUN_0822c434`, который разбирает шестибайтовую
позицию и зовёт `FUN_08cceb48`:

```
FUN_0822c434: вызвать внутренний переход с *(u32*)поз и *(u16*)(поз+4)
FUN_08cceb48: если номер блока не тот, что закеширован в +0x14 — загрузить блок;
              затем шагать записями вперёд, пока ключ не совпадёт с искомым
```

Ключевое: второе число — **не байтовое смещение**, а ключ записи. Переход
реализован обходом: загрузить блок и идти записями, сравнивая ключ
(`FUN_08ccecdc` даёт текущий, `FUN_08ccecd8` шагает к следующему). Есть и
ветка перехода в соседний блок, когда ключ больше всех в текущем.

### Проверка: байтовое смещение отвергнуто

Пары, прочитанные из узла дерева, проверены как (блок, смещение):

| пара | что по этому адресу |
|---|---|
| (99245, 4381) | байты `44 52 45 41 4d` — текст `DREAM` |
| (99245, 4382) | те же байты со сдвигом на один |
| (99245, 4443) | середина записи имён |
| (99255, 26972) | **за концом блока** (в блоке 15 883 байта) |

Текст на месте узла дерева и выход за границу блока — этого достаточно, чтобы
гипотезу закрыть. Либо пары прочитаны неверно, либо порядок чисел другой; но
байтовым смещением второе число не является в любом случае, это видно из кода
перехода.

## Диспетчер типов: байт в начале записи

`КОНЕЦ -> слово 180` — это возврат на **правило 17**, а правила 17…41 оказались
диспетчером. Правило 18 читает байт, дальше 22 условных перехода, и правило 41
с кодом `0xc3` — «тип не опознан». Кода `0xc3` в схеме ровно один, так что все
наши «рассинхронизации» приходят именно отсюда.

Отсюда полная таблица: какой байт какой записью объявляется.

| байт | структура | байт | структура |
|---|---|---|---|
| `< 0x80` | `0x07` | `0x98` | `0x02` |
| `0x80` | `0x80` | `0x99` | `0x25` |
| `0x90` | `0x01` | `0xa0…0xa7` | `0x03` |
| `0x91` | `0x20` | `0xa8…0xaf` | **`0x24` точка POI** |
| `0x92` | `0x18` | `0xb0…0xb7` | `0x06` |
| `0x93` | `0x04` | `0xb8…0xbf` | `0x10` |
| `0x94` | **`0x21` заголовок слоя** | `0xc8…0xcf` | `0x12` |
| `0x95` | **`0x22` узел дерева** | `0xd0…0xd7` | `0x30` |
| `0x96` | **`0x23` тайл** | `0xd8…0xdf` | `0x32` |
| `0x97` | `0x34` | `0xe0…0xe7` | `0x31` |
| | | `0xe8…0xef` | `0x05` |
| | | `0xf0…0xf7` | `0x33` |

Не покрыты и ведут на ошибку: `0x81…0x8f`, `0x9a…0x9f`, `0xc0…0xc7`,
`0xf8…0xff`.

Это даёт прямой ход к точкам интереса: узел дерева объявляется байтом `0x95`,
заголовок слоя `0x94`, тайл `0x96`. Их можно искать в сырых байтах, не полагаясь
на то, что машина дойдёт до них сама.

Младшие биты байта у структур с диапазоном (`0x24` это `0xa8…0xaf`) — это
флаги, по которым идут условия в правилах. У `0x07`, занимающего весь диапазон
ниже `0x80`, флагов семь.

## Где машина расходится: структура `0x07`

Замер по 1308 прогонам (склейки по всему каталогу): до конца данных доходит
**14.6 %**, остальное — остановки.

| причина | случаев |
|---|---|
| конец (`0x11` без цели) | 305 |
| **рассинхронизация после `0x07`** | 288 |
| **рассинхронизация после `0x1002`** | 238 |
| данные кончились | 191 |
| чтение за концом блока | 95 |
| прочее | 181 |

Две главные причины — одна и та же. Правило 348 внутри `0x07` вызывает
структуру `0x1002`, когда у байта взведён бит 4; тогда падение приписывается
`0x1002`, иначе — самой `0x07`. Сложив их, получаем: **526 из 1308 прогонов
ломаются внутри разбора `0x07`** (правила 342…355).

### Отвергнуто: это не постоянный сдвиг

Первая догадка — что мы читаем на байт больше или меньше. Проверено линейкой:
для каждого падения искался ближайший сдвиг, при котором байт стал бы
осмысленным типом. Ни одного преобладающего значения нет: для `0x07` это
«нет подходящего» 19 %, `+1` 19 %, `−1` 15 %, `+2` 11 %, `−2` 10 %, дальше
хвост. Значит ошибка накапливается и зависит от ветки, а не от постоянного
смещения.

### Отвергнуто: бит 4 как признак поломки

Сравнение байтов удачных записей `0x07` с последними перед падением дало
резкий на вид результат: бит 4 стоит у 47.8 % удачных и у **0.0 %** сломанных.
Выглядит как указание на виновника, но это артефакт учёта: при взведённом
бите 4 выполняется вызов `0x1002`, и падение записывается уже на её счёт.
Никакого сигнала здесь нет.

Прочие биты сигнала не дают вовсе: бит 2 — 46.9 % против 42.7 %, бит 3 —
37.5 % против 38.9 %, бит 5 — 37.7 % против 29.5 %.

## Якорь: Эйфелева башня

Приём, который трижды сработал в `XAC`, — взять данные с заведомо известным
содержимым. Поиск `EIFFEL` по сырым байтам всех четырёх томов даёт попадания в
каждом:

| блок | что вокруг |
|---|---|
| 43326 | `GUST&.EIFFEL-4|av|'ny||gys|'tav||E|'fEl|` — улица с транскрипцией |
| 53405 | `ST..EIFFEL+4|a|'le||gys|'tav||E|'fEl|` — то же |
| **243283** | `HUTTI BAR`, `GLUSCO`, `DI POMODORI`, **`EIFFEL`**, `HALL`, `CITY`, `INFINITY` |

Блок 243283 — это область точек интереса (записи слоёв начинаются примерно с
блока 242 520), и рядом с `EIFFEL` стоят названия заведений. Якорь найден:
**блок 243283, байт 14402**.

Воспользоваться им пока не вышло. Перебор 300 точек старта с правила 17 не дал
ни одной, которая вывела бы разбор ровно на 14402; лучшие по числу записей
оказались вырождением — 136 одинаковых `0x1001` подряд. Штатный проход по этому
блоку умирает на позиции 849 из 16 865. То есть машина ломается задолго до
якоря, и подбирать старт бессмысленно, пока не починена сама причина.

## Заголовок блока подтверждён с двух концов

Правила 0…16 — это заголовок блока, и он занимает ровно 13 байт:

| байт | правило | что |
|---|---|---|
| 0…1 | 1 | число, тип `0x46` |
| 2 | 2 | байт, тип `0x29` |
| **3** | **3** | **ширина координат** |
| 4…7 | 4 | знаковое 4 байта, тип `0x25` |
| 8…11 | 5 | знаковое 4 байта, тип `0x26` |
| 12 | 6 | число словарных записей |
| 13… | 8…15 | цикл: сам словарь |

Сходится с двух концов: наш `src/litdict.js` независимо читает число словарных
записей из `block[12]`, а данные словаря с `block[13]` — ровно там, где их
кладёт грамматика.

### Ширину задаёт единственное правило

Во всей схеме из 393 правил ширину координат выставляет **одно** — правило 3.
Прошивка кладёт её в поле `+0xc` состояния разбора (`*(short *)(piVar24 + 3)`)
и оттуда же читает в коде `0x40`. Поддержаны ровно четыре значения — 8, 12, 16
и 24; при любом другом координата читается как `(0,0)` **без сдвига по
потоку**. Наш обработчик совпадает с этим побайтово.

Отсюда следствие: при старте разбора с середины блока ширина остаётся
умолчательной, и все упакованные пары читаются неверно. Для опытов в
`src/litvm.js` добавлен параметр `wid`.

## Ещё три гипотезы проверены и отвергнуты

### Мусорная ширина — причина поломки

Байт 3 равен 8, 12, 16 или 24 только у 22 % блоков. Казалось бы, вот и причина.
Замер связи с успехом разбора выглядит убедительно:

| блоки со своим словарём | штук | дочитано до конца |
|---|---|---|
| ширина 8/12/16/24 | 272 | 64 (**23.5 %**) |
| ширина иная | 358 | 6 (**1.7 %**) |

Разница в четырнадцать раз. Но проверка прямая: если ширина и правда причина,
подстановка верной должна помочь. Не помогает. На 233 блоках с мусорным
байтом 3 перебор всех четырёх значений даёт **1.7 % при каждом** — ровно
столько же, сколько без подстановки, и это одни и те же 4 блока.

Значит байт 3 — не причина, а **признак**: там, где он осмыслен, блок того
вида, который мы умеем читать.

### Расхождение в условии записи в поле

В прошивке присваивание в поля состояния (`switch` по `w1`) выполняется только
при `iVar18 == 2`, то есть когда предыдущая операция дала **число**. У нас
`store` вызывается при `val !== null`. Выглядело как расхождение.

Проверено по всем кодам: `0x80` (константа), `0x81` (чтение из регистра),
`0x60`…`0x64` (аргумент вызова, номера витков) — все ставят `iVar18 = 2`;
`0x40` ставит 4, `0x41` — 3, `0x44` — 6, `0x65` — 3, пропуск — 7. Это в
точности наше условие. Расхождения нет.

### Подбор точки старта по якорю

Описан выше: 300 проб, ни одного попадания, лучшие результаты — вырождение.

## Исправление пустого цикла `0xa2` (20 сентября 2026)

В `FUN_08cd0798` нулевые циклы `0xa0` и `0xa2` обрабатываются **по-разному**.
У `0xa0` поиск сравнивает текущее правило с `0xa1`, затем общий шаг переходит
за него. У `0xa2` поиск сравнивает **следующее** правило
`puVar23[stride]` с `0x11`, поэтому останавливается перед ним. Общий шаг
переходит на саму `0x11`: она должна исполнить переход по `w4` либо возврат
из подпрограммы.

В нашей машине обе ветки переходили за завершающее правило. При пустом
`0xa2` терялся переход/возврат, и исполнение проваливалось в соседнюю структуру.
Исправлена только эта ветка в `src/litvm.js`. Прежнее утверждение раздела
«Циклы были написаны неверно» о переходе за конец обоих циклов неверно для `0xa2`.

| Проверка | До | После |
|---|---:|---:|
| PIT: точное достижение конца блока | 704/717 | **717/717** |
| LIT: точное достижение конца блока в выборке | 26/218 | **64/218** |
| LIT: прежде успешные блоки, переставшие доходить до конца | — | **0** |
| LIT, блок 1000: позиция остановки | 5028/44460 | **44460/44460** |

**Поправка (в тот же день, ниже).** Выборка «26/218 → 64/218» взята не по той
совокупности: индексы шли равномерно по всему каталогу, а грамматика с правила 0
применима лишь к его первой пятой. Сам вывод о `0xa2` верен и подтверждён
прошивкой, а вот эти два числа сравнивать с поздними нельзя. По зоне
заголовков та же правка даёт 148/601 → 388/601.

Выборка LIT: индексы `0, 613, 1226, …` до конца каталога, отбор существующей
функцией `litdict.dict(block) !== null`. Этот отбор — эвристика, не гарантия
правильного заголовка. Читаются отдельные блоки, без склейки продолжений.
Цифры нельзя переносить на весь LIT или сравнивать напрямую с прежними
процентами качества названий. «Успех» здесь означает одновременно
`why === 'данные кончились'` и `pos === block.length`; выход за границу успехом
не считается. Даже точная граница не доказывает правильность полей.

Воспроизведение: `node src/litvmcheck.js`. Для сравнения с прежней машиной
передать её файл первым аргументом. Проверка читает всю небольшую PIT и только
фиксированную выборку LIT; контрольные суммы набора не запускаются.
`node --test src/litvm.test.js` проверяет пустой цикл с переходом, возврат
из подпрограммы, непустой `0xa2` и сохранение поведения пустого `0xa0`.

**Блок 243283 ещё не исправлен:** остановка остаётся на байте 849/16865
после `0x07`. Склейка с блоком 243284 (до следующего заголовка по эвристике
словаря) даёт ту же остановку 849/34483. Следовательно, одной нехваткой
продолжения эта конкретная остановка не объясняется. Байт ширины в начале
243283 равен `0xfc`; успешное чтение предполагаемого словаря само по себе
не подтверждает, что это корректная точка входа в грамматику заголовка.
Обход дерева POI и генератор LIT по-прежнему не завершены.

## «Каталог LIT зонирован» — наблюдение верное, объяснение неверное

**Этот раздел опровергнут следующим.** Зонирования нет: заголовок есть у всех
245 518 блоков. То, что выглядело границей зоны на блоке 53404, — шов между
томами L1 и L2, посчитанный неверно. Раздел оставлен целиком, потому что
проверка «есть ли заголовок» из него верна и именно она вывела на шов.

## Каталог LIT зонирован: заголовок есть только у первой пятой блоков

Грамматика начинается структурой-заголовком (правила 0…16): `u16`, байт, **байт
ширины координат**, два `s32`, число записей словаря, сам словарь, и переход
`0x11 → 17` в диспетчер. Значит у блока, к которому применима грамматика с
правила 0, байт 3 обязан быть шириной, которую понимает код `0x40`.

Ширин ровно четыре — `FUN_08cd0798` разбирает `8`, `0xc`, `0x10`, `0x18`, а на
всех прочих значениях ставит `x = y = 0` **и не читает ни одного байта**. Отсюда
дешёвая и точная проверка «есть ли заголовок»: `байт2 == 0 && байт3 ∈ {8,12,16,24}`.

Проход по каталогу этой проверкой даёт резкую границу:

```
блоки 0 … 53403     заголовок есть у всех подряд   (ширина 24 у ~82 %, 16 у ~18 %)
блок  53404         первый без заголовка
блоки 53404 … 245517  заголовка нет, байт 3 разбросан по всему диапазону
```

То есть заголовок есть у **53 404 блоков из 245 518 — 21.8 % каталога**. Граница
не размыта: на сотне блоков до неё заголовок у всех, на сотне после — ни у кого.

**Следствие для всех прежних замеров.** Равномерная выборка по каталогу на три
четверти состоит из блоков, к которым грамматика с правила 0 неприменима, и
меряет не машину, а выбор точки входа. `src/litvmcheck.js` переписан: зона
находится двоичным поиском по той же проверке заголовка, выборка берётся внутри
неё.

**Поправка к разделу «Ширина координат берётся из состояния разбора».** Там
сказано, что ширина «берётся из данных и часто выходит мусорной» (18 621 случай
на 200 склейках, значения 123, 144, 41, 0, 67, 245). Ширину задаёт **ровно одно
правило схемы — номер 3**, и никакое другое: перебор всех 393 правил даёт
единственное с `w1 = 3`. Внутри правильно начатого блока ширина постоянна и
мусорной стать не может. Те мусорные значения — это байт 3 блоков **без**
заголовка, то есть симптом запуска грамматики не с той точки, а не расхождения
внутри разбора. Отдельной задачи «починить ширину» нет.

**Блок 243283 объяснён.** Прежняя запись оставляла его остановку на 849/16865
загадкой. Он лежит на позиции 243 283 — далеко за границей зоны, байт ширины у
него `0xfc`. Заголовка у него нет, поэтому запуск с правила 0 к нему просто не
относится; вопрос был поставлен неверно. Чем этот блок является — ниже.

**Чем заняты остальные 191 тысяча блоков — неизвестно.** Проверено: вход прямо в
диспетчер (правило 17, с байта 0) при каждой из четырёх ширин доводит до конца
лишь 11…14 блоков из 301 в выборке по зоне без заголовков и 3…7 из 302 в хвосте
каталога, где документ выше нашёл записи дерева `0x21`/`0x22`/`0x23`. Значит это
и не «тот же формат без заголовка». Это следующая цель, и обход дерева POI
упирается именно в неё.

## Кадр грамматики чистится целиком — при вызове и при переходе `0x11`

Это оказалось главной причиной расхождений внутри блоков с заголовком.

В `FUN_08cd0798` кадр уровня — это область, начинающаяся словом `уровень*8 + 7`.
Раскладку удалось прочитать целиком, потому что её обнуляет одна маленькая
функция `FUN_08cd066c`, и каждое её поле опознаётся по тому, кто это же смещение
читает и пишет в разборщике:

| смещение от начала кадра | поле | кто пользуется |
|---|---|---|
| +0 | указатель схемы | общий шаг `puVar12 + stride` |
| +4 | **регистр** | `w1 = 4…7`, условия, `0x81`, `0xc2` |
| +8 | счётчик цикла `0xa0` | `w1 = 1`, ветка `0xa0` |
| +10 | поле f38 | `w1 = 2`, длина у `0x42` |
| +12 | счётчик цикла `0xa2` | `w1 = 8`, ветки `0xa2` и `0x11` |
| +14 | длина строки | `w1 = 9`, `0x43`, `0x44` |
| +16 | **аргумент вызова** | `0x60`, `0x61`, `0x62` |
| +20 | сохранённое правило `0xa2` | возврат на виток |
| +24 | сохранённое правило `0xa0` | возврат на виток |
| +28 | номер витка `0xa2` | `0x63` |
| +30 | номер витка `0xa0` | `0x64` |

`FUN_08cd066c` зануляет всё это, ставит оба номера витка в `-1`, а указатель
схемы — в свой второй аргумент. Поле **+16 она намеренно пропускает**: аргумент
кладут отдельной строкой сразу после вызова.

Вызывается она ровно из двух мест, и оба адреса в литеральном пуле
(`PTR_FUN_08cd11a4` и `PTR_FUN_08cd0c20`) указывают на неё же — `0x08cd066c`:

* **вызов `0xc1`/`0xc2`** — по кадру нового уровня, со вторым аргументом `0`;
* **`0x11` с ненулевым `w4`** — по кадру **текущего** уровня, со вторым
  аргументом «цель минус шаг».

Второе и важно: `0x11 → 17` стоит в конце структур `0x7` и `0x31` и в конце
заголовка, то есть **между записями**. Регистр и состояние циклов там
обнуляются, а наша машина их сохраняла. Условия следующей записи читали биты,
оставшиеся от предыдущей, число прочитанных байт «плыло», и разбор рано или
поздно приносил диспетчеру байт-тег, которого тот не знает.

**Порядок проверок в `0x11` тоже был обратным.** Прошивка смотрит `w4` **раньше**
уровня: непустой `w4` — это переход, он идёт по цели и не возвращает из
подпрограммы, и только при `w4 == 0` уровень уменьшается. Наша машина сперва
снимала кадр вызова.

Обе правки нужны вместе — порознь каждая даёт заметно меньше:

| вариант машины | LIT, зона заголовков (выборка 601) | PIT (717) |
|---|---:|---:|
| коммит `540bf9e` | 148 | 704 |
| + выход из пустого `0xa2` | 388 | 717 |
| + только чистка кадра при вызове | 388 | 717 |
| + только чистка кадра при переходе `0x11` | 555 | 717 |
| **обе чистки и порядок в `0x11`** | **601** | **717** |

По всей зоне целиком, все 53 404 блока подряд: **33 847 → 53 400**, прежде
успешных и ставших неуспешными — **0**.

Названия при этом не пострадали: на выборке каждого 613-го блока зоны прочитано
102 057 названий против 82 694, из них проходят проверку на печатаемость 67 927
против 60 149. Доля падает с 72.7 % до 66.6 % просто потому, что читаются ранее
недоступные участки; сравнивать эту долю с «96.4 %» из раздела о координатах
нельзя — там другая мерка чистоты.

### Что ещё сверено с прошивкой по дороге

Проверено построчно и **совпало** с нашей машиной, переделывать нечего: кодек
`0x41` во всех ветках (включая `0xff`, `0x0c`…`0x0f` и знак у 12-битной дельты),
`0x40` во всех четырёх ширинах, `0x81` (сдвиг из `w4`, маска из `w5`), аргумент
`0xc2` (сдвиг `w5 >> 8`, ширина `w5 & 0x1f`), `0x80`, `0x60`…`0x64`, `0x42`,
`0x43`, `0x44`, пропуск правил с битом 14 после перехода, таблица масок
разрядности. Байты escape у беззнакового varint — 252, 253, 254: прочитаны из
литерального пула (`DAT_08cd0c1c = 253`, `DAT_08cd0c1e = 254`). Оттуда же
подтверждены коды операций: `DAT_08cd0a6c = 0x80`, `0a6e = 0xa2`, `0a70 = 0xc2`,
`0a72 = 0xc1`, `0a74 = 0xc3`, `DAT_08cd1140 = 0xa1`.

Отдельно подтверждена правка Codex по пустому `0xa2`: поиск действительно
сравнивает **следующее** правило `puVar23[stride]` с `0x11` и останавливается
перед ним, тогда как у `0xa0` цикл `do…while` сравнивает уже текущее правило с
`0xa1` и останавливается на нём. Общий шаг `puVar12 + stride` и даёт разницу в
одно правило.

Знаковый varint `0x20` в прошивке устроен **иначе** беззнакового: escape-байты
там `0x83` (3 байта), `0x82` (4 байта), `0x84` (2 байта), и многобайтовые
значения не расширяются знаком. В нашей машине он написан как беззнаковый, но
в грамматике LIT кода `0x20` нет вовсе (как и `0x21`, `0x22`, `0x23`, `0x28`),
так что на разбор это не влияет. Оставлено как есть, помечено здесь.

### Отвергнутые гипотезы (с цифрами, чтобы не проверять снова)

* **`0x10` обнуляет регистр.** Нет. `FUN_08cd066c` из `0x10` не вызывается, а
  сам код `0x10` сбрасывает только номера витков, счётчик `0xa2` и сохранённые
  правила. Замер: зона заголовков падает с 388/601 до **7/601**.
* **Кадр вызываемого сохраняется между вызовами** (прежний комментарий в
  `src/litvm.js`: «вызов НЕ обнуляет их — он только кладёт аргумент»). Неверно,
  см. выше.
* **Ширина координат ломается по ходу разбора.** Нет, её задаёт единственное
  правило 3; см. поправку выше.
* **Ветка ширины 16 в коде `0x40` написана неверно.** Нет: в прошивке это
  `CONCAT11` дважды, то есть два больших-endian `u16`, ровно как у нас. Перекос
  «ширина 16 отказывает в 64 случаях из 80, ширина 24 — в 86 из 320» был
  следствием чистки кадра, а не ширины: после правки обе ширины дают 100 %.
* **Расхождение — это один постоянный сдвиг в байтах.** Нет. Поиск поправки в
  пределах ±8 байт от места остановки с перезапуском диспетчера починил лишь
  46 блоков из 120, а сами поправки распределились по всему диапазону от −8 до
  +8 без пика. Приём пересинхронизации при этом исправен: на заведомо целых
  блоках вход с правила 17 после словаря доводит до конца 39 из 40.

### Воспроизведение

```
node src/litvmcheck.js                  # границы: зона заголовков LIT и вся PIT
node src/litvmcheck.js <прежний litvm.js>  # ещё и сравнение с прежней машиной
node --test src/litvm.test.js           # 8 проверок управления потоком
```

`litvmcheck.js` читает только выборку и не считает контрольные суммы набора.
«Успех» — это одновременно `why === 'данные кончились'` и `pos === block.length`;
выход за границу успехом не считается. Точная граница не доказывает, что поля
разобраны правильно, — она доказывает только, что число прочитанных байт сошлось.

## Тома склеивались неверно — и это скрывало три четверти каталога

Раздел выше объявил каталог зонированным: заголовок есть у блоков 0…53403 и
нет ни у одного дальше. Наблюдение верное, вывод — нет. Граница села **ровно на
шов между томами L1 и L2**, и это её единственная причина.

### Что было не так

`lit.js` складывал тома встык, считая длину каждого как «размер файла минус
обёртка FLDB (2048 байт)». Но файл тома **добит нулями до кратности 2048**, и
эта добивка в сквозное адресное пространство не входит. У тома 1 добивки
1436 байт — ровно на столько разъезжается всё, что лежит дальше.

Видно это в упор. Блок 53404 по наивной арифметике начинается за 1436 байт до
конца тома 1, и там одни нули:

```
блок 53403  @2147360048  размер 38196  первые байты 0a77 0018 0000...   заголовок ✓
блок 53404  @2147398244  размер 37504  первые байты 0000 0000 0000...   нули — добивка
сразу за швом (начало данных тома 2):     0b78 0018 0000 0000 d0124520  заголовок ✓
```

То есть нетронутый заголовок блока лежал сразу за швом, а мы читали хвост
предыдущего файла.

### Как шов вычисляется точно

Гадать не нужно, шов выводится из самого каталога двумя ограничениями:

1. блок физически лежит **в одном файле**, значит шов обязан совпасть с началом
   какого-то блока;
2. добивка меньше 2048 байт, значит шов лежит в окне последних 2048 адресов
   тома.

На всех трёх швах в это окно попадает **ровно по одному** началу блока —
проверка сходится сама с собой, выбирать не из чего:

| том | размер файла | окно поиска | кандидатов | данных в томе | добивка |
|---|---:|---|---:|---:|---:|
| 1 | 2 147 401 728 | (2147397632, 2147399680] | **1** (блок 53404) | 2 147 398 244 | 1436 |
| 2 | 2 147 383 296 | (4294777444, 4294779492] | **1** (блок 134719) | 2 147 380 286 | 962 |
| 3 | 2 147 401 728 | (6442176162, 6442178210] | **1** (блок 211207) | 2 147 397 990 | 1690 |
| 4 | 631 121 920 | — | — | 631 119 872 | — |

Сходится и с другого конца: при новых швах адресное пространство кончается на
7 073 296 392, а последний блок — на 7 073 296 007, то есть хвост 385 байт,
опять меньше 2048. Реализовано в `lit.js` как `fixSeams`; каталог целиком лежит
в первом томе, поэтому читается ещё по наивным границам и сам же их уточняет.

### Что это меняет

| | до | после |
|---|---:|---:|
| блоков с валидной шириной координат | 53 404 из 245 518 (21.8 %) | **245 518 из 245 518** |
| до точной границы, выборка по всему каталогу | 148/601 | **601/601** |
| до точной границы, **весь каталог подряд** | — | **245 518 из 245 518** |

Оговорка к признаку. «Байт 3 — валидная ширина» это признак правильно начатого
блока, а не определение заголовка. На LIT он работает (21.8 % -> 100 %), потому
что координаты там читаются. У PIT то же поле валидно лишь у 13 блоков из 717,
а до точной границы доходят все 717: на пути разбора PIT кода `0x40` просто нет,
и ширина ни на что не влияет.

**Зонирования нет.** Нет и «191 тысячи блоков неизвестного назначения», и
отдельной задачи «разобрать вторую зону» — всё это было одной ошибкой в
арифметике склейки. Заодно отпадает склейка продолжений в `litnames.js`
(`groups`): она собирала блоки без заголовка к предыдущему с заголовком, а
блоков без заголовка не существует — каждый блок самодостаточен, со своим
словарём.

**Осторожно с прежними цифрами.** Всё, что меряло LIT за пределами тома 1 —
доли чистых имён, статистика ширин координат, поиск записей дерева в хвосте
каталога, — считалось по съехавшим данным. Числа из тех разделов
воспроизводить нельзя.

## Длины и счётчики читаются без знака

Последние четыре блока тома 1 (26, 27, 28, 29 — самые крупные, около 62 700
байт) не сходились даже после всех правок выше, а у блока 28 позиция остановки
была **отрицательной**: −3906.

В прошивке поля кадра кладутся с обрезкой `(short)`, но **читаются без знака**:
длины в `0x42`, `0x43` и `0x44` берутся как `*(ushort *)`, а пределы циклов в
`0xa1` и `0x11` сравниваются через `(int)(uint)`. У нас стояло знаковое
расширение, поэтому длина `0xffff` превращалась в −1 и указатель уезжал назад
по буферу — разбор начинал перечитывать уже прочитанное.

Обрезка без знака совпадает с прошивкой во всех местах чтения: сравнение
счётчика с нулём (`0xa0`, `0xa2`) от знака не зависит. После правки эти четыре
блока сходятся, PIT не затронута.

## Проверка содержимым: имена читаются чисто

Сходимость по длине — слабая проверка: она говорит лишь, что число прочитанных
байт совпало с размером блока. Содержимое проверяется отдельно и независимо —
названиями.

На выборке из 401 блока, равномерно по всему каталогу, прочитано **550 482
названия**, из них проходят проверку на печатаемость **550 469 — все, кроме 13**.
Ни одно название не отброшено из-за управляющих байтов; словарь разобрался у
всех 401 блока.

Мерку пришлось сначала исправить самому. Первый замер дал 54.4 %, и отбракованные
названия выглядели так:

```
"BERGSTRASSE%OBERE"   "WORTH%AUF DER"   "BOEHLE-WEG%ADOLF-"
```

Это не мусор, а сортировочная запись NavTeq: `%` отделяет основу от приставки,
`BERGSTRASSE%OBERE` читается как «Obere Bergstraße». Знака `%` не было в наборе
допустимых символов — ошибка была в проверке, а не в разборе.

Две независимые проверки сходятся: границы блоков совпадают у 245 518 из
245 518, и содержимое этих блоков — осмысленные названия. Это уже не
правдоподобный разбор, а разбор, сошедшийся с двух концов.
